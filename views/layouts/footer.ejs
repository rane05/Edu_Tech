<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
<script src="https://unpkg.com/locomotive-scroll/dist/locomotive-scroll.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@mojs/core"></script>

<footer id="contact" class="footer">
  <style>
    .footer {
      background: linear-gradient(135deg, var(--dark-900) 0%, var(--dark-800) 100%);
      color: var(--gray-100);
      padding: var(--space-16) 0 var(--space-8);
      position: relative;
      overflow: hidden;
    }

    .footer::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, var(--color-primary), var(--color-secondary), var(--color-primary));
      animation: gradientShift 3s ease infinite;
    }

    .footer-content {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 var(--space-6);
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 2fr;
      gap: var(--space-8);
    }

    .footer-section h3 {
      color: var(--color-primary);
      font-size: var(--text-xl);
      font-weight: var(--font-bold);
      margin-bottom: var(--space-4);
      position: relative;
    }

    .footer-section h3::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 40px;
      height: 2px;
      background: var(--color-secondary);
    }

    .footer-section p {
      color: var(--gray-400);
      line-height: var(--leading-relaxed);
      margin-bottom: var(--space-4);
    }

    .footer-links {
      list-style: none;
      padding: 0;
    }

    .footer-links li {
      margin-bottom: var(--space-2);
    }

    .footer-links a {
      color: var(--gray-300);
      text-decoration: none;
      transition: all var(--transition-normal);
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
    }

    .footer-links a:hover {
      color: var(--color-primary);
      transform: translateX(4px);
    }

    .footer-links a i {
      font-size: var(--text-sm);
      opacity: 0.7;
    }

    .contact-info {
      list-style: none;
      padding: 0;
    }

    .contact-info li {
      display: flex;
      align-items: center;
      gap: var(--space-3);
      margin-bottom: var(--space-3);
      color: var(--gray-300);
    }

    .contact-info i {
      color: var(--color-primary);
      font-size: var(--text-lg);
      width: 20px;
      text-align: center;
    }

    .newsletter-form {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .newsletter-input {
      padding: var(--space-3) var(--space-4);
      border: 1px solid var(--gray-600);
      border-radius: var(--radius-lg);
      background: var(--dark-700);
      color: var(--gray-100);
      font-size: var(--text-base);
      transition: all var(--transition-normal);
    }

    .newsletter-input:focus {
      outline: none;
      border-color: var(--color-primary);
      box-shadow: 0 0 0 3px rgba(255, 184, 0, 0.1);
    }

    .newsletter-input::placeholder {
      color: var(--gray-500);
    }

    .btn-newsletter {
      background: var(--color-primary);
      color: var(--dark-900);
      padding: var(--space-3) var(--space-6);
      border: none;
      border-radius: var(--radius-full);
      font-weight: var(--font-semibold);
      cursor: pointer;
      transition: all var(--transition-normal);
      box-shadow: 0 4px 12px rgba(255, 184, 0, 0.2);
    }

    .btn-newsletter:hover {
      background: var(--primary-600);
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(255, 184, 0, 0.3);
    }

    .social-links {
      display: flex;
      gap: var(--space-3);
      margin-top: var(--space-4);
    }

    .social-links a {
      width: 40px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--dark-700);
      color: var(--gray-300);
      border-radius: 50%;
      text-decoration: none;
      transition: all var(--transition-normal);
      border: 1px solid var(--gray-600);
    }

    .social-links a:hover {
      background: var(--color-primary);
      color: var(--dark-900);
      transform: translateY(-3px);
      box-shadow: 0 4px 12px rgba(255, 184, 0, 0.3);
    }

    .footer-bottom {
      max-width: 1280px;
      margin: 0 auto;
      padding: var(--space-6) var(--space-6) 0;
      border-top: 1px solid var(--gray-700);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: var(--space-4);
    }

    .footer-bottom p {
      color: var(--gray-400);
      margin: 0;
    }

    .back-to-top {
      background: var(--color-primary);
      color: var(--dark-900);
      padding: var(--space-2) var(--space-4);
      border-radius: var(--radius-full);
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: var(--space-2);
      font-weight: var(--font-semibold);
      transition: all var(--transition-normal);
      box-shadow: 0 4px 12px rgba(255, 184, 0, 0.2);
    }

    .back-to-top:hover {
      background: var(--primary-600);
      transform: translateY(-2px);
      box-shadow: 0 6px 16px rgba(255, 184, 0, 0.3);
    }

    @media (max-width: 768px) {
      .footer-content {
        grid-template-columns: 1fr;
        gap: var(--space-6);
        text-align: center;
      }

      .footer-section h3::after {
        left: 50%;
        transform: translateX(-50%);
      }

      .contact-info li {
        justify-content: center;
      }

      .social-links {
        justify-content: center;
      }

      .footer-bottom {
        flex-direction: column;
        text-align: center;
      }
    }
  </style>

  <div class="footer-content">
    <div class="footer-section">
      <h3>About EduNavigator</h3>
      <p>EduNavigator is your ultimate guide to exploring careers, finding the right education path, and achieving your professional goals.</p>
      <ul class="footer-links">
        <li><a href="#">Home</a></li>
        <li><a href="#">Careers</a></li>
        <li><a href="#">About Us</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#">Privacy Policy</a></li>
      </ul>
    </div>

    <div class="footer-section">
      <h3>Quick Links</h3>
      <ul class="footer-links">
        <li><a href="#">Home</a></li>
        <li><a href="#">Careers</a></li>
        <li><a href="#">About Us</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#">Privacy Policy</a></li>
      </ul>
    </div>

    <div class="footer-section">
      <h3>Contact Us</h3>
      <ul class="contact-info">
        <li><i class="fas fa-map-marker-alt"></i> 123 Edu Street, Knowledge City, India</li>
        <li><i class="fas fa-phone"></i> +91 123 456 7890</li>
        <li><i class="fas fa-envelope"></i> info@edunavigator.com</li>
      </ul>
    </div>

    <div class="footer-section">
      <h3>Subscribe to Our Newsletter</h3>
      <p>Get the latest updates on careers, education, and more.</p>
      <form class="newsletter-form">
        <input type="email" class="newsletter-input" placeholder="Enter your email">
        <button class="btn-newsletter">Subscribe</button>
      </form>
    </div>
  </div>

  <div class="footer-bottom">
    <p>&copy; 2025 EduNavigator. All Rights Reserved.</p>
    <div class="social-links">
      <a href="#"><i class="fab fa-facebook-f"></i></a>
      <a href="#"><i class="fab fa-linkedin-in"></i></a>
      <a href="#"><i class="fab fa-instagram"></i></a>
      <a href="#"><i class="fab fa-twitter"></i></a>
      <a href="#"><i class="fab fa-youtube"></i></a>
    </div>
    <a href="#top" class="back-to-top"><i class="fas fa-arrow-up"></i> Back to Top</a>
  </div>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>