<%- include('layouts/header'); %>
  <%- include('layouts/navbar'); %>

<<<<<<< HEAD
    <style>
      :root {
        /* V6 Premium SaaS Layered Palette */
        /* Global Platform Theme Remapping (V7 - Yellow Theme) */

        /* Backgrounds */
        --c-cream: #F7FAFC;
        /* Global light bg */
        --c-cream-alt: #FFFFFF;
        /* Global white */
        --c-base: #FFFFFF;
        /* Card surfaces */

        /* Typography */
        --c-navy: #1A1A2E;
        /* Global dark text */
        --c-navy-muted: #2D3748;
        /* Global dark secondary text */
        --c-slate: #4A5568;
        /* Global standard body text */
        --c-slate-light: #718096;
        /* Global light text */

        /* Accents mapped to Classic Yellow Theme */
        --c-indigo: #FFB800;
        /* Primary Yellow */
        --c-indigo-soft: rgba(255, 184, 0, 0.1);
        --c-teal: #00A8FF;
        /* Secondary Blue */
        --c-teal-soft: rgba(0, 168, 255, 0.1);

        /* Secondary Accents */
        --c-amber: #6C63FF;
        /* Accent Purple */
        --c-amber-soft: rgba(108, 99, 255, 0.1);
        --c-coral: #10B981;
        /* Success Green */
        --c-coral-soft: rgba(16, 185, 129, 0.1);

        /* Ambient Glows Adjusted for new palette */
        --c-indigo-glow: rgba(255, 184, 0, 0.15);
        /* Yellow glow */
        --c-teal-glow: rgba(0, 168, 255, 0.12);
        /* Blue glow */
        --c-amber-glow: rgba(108, 99, 255, 0.08);
        /* Purple glow */
        --c-coral-glow: rgba(16, 185, 129, 0.08);
        /* Green glow */
        --c-tint-indigo: #FFFaf0;
        /* Yellow tint */
        --c-tint-lavender: #f0f8ff;
        /* Blue tint */
        /* Spacing System (Maintained from V5) */
        --space-section-ds: 120px;
        --space-section-tb: 80px;
        --space-section-mb: 64px;
        --gap-xl: 48px;
        --gap-lg: 32px;
        --gap-md: 24px;
        --gap-sm: 16px;
        --card-pad: 32px;

        /* V6 Refined Shadows (Color hinted) */
        --shadow-micro: 0 1px 2px rgba(15, 23, 42, 0.04);
        --shadow-soft: 0 4px 12px rgba(15, 23, 42, 0.05), 0 1px 3px rgba(15, 23, 42, 0.03);
        --shadow-float: 0 24px 48px rgba(15, 23, 42, 0.08), 0 8px 16px rgba(15, 23, 42, 0.03);
        --shadow-base-ui: 0 4px 12px rgba(15, 23, 42, 0.06);
        --shadow-glass: 0 8px 32px rgba(15, 23, 42, 0.05);

        /* Section Specific Drop Shadows for Cards */
        --shadow-glow-indigo: 0 12px 32px rgba(99, 102, 241, 0.08);

        /* Motion Focus */
        --dur-hover: 280ms;
        --dur-reveal: 400ms;
        --dur-chart: 800ms;
        --dur-float: 6s;
        --ease-out-smooth: cubic-bezier(0.16, 1, 0.3, 1);
        --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);

        --bg-noise: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='1'/%3E%3C/svg%3E");
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Inter', 'SF Pro Display', sans-serif;
        background-color: var(--c-cream);
        /* V6: Removed pure white #FFF globally */
        color: var(--c-navy);
        -webkit-font-smoothing: antialiased;
        overflow-x: hidden;
      }

      /* Base Realism Overlay */
      .noise-overlay {
        position: fixed;
        inset: 0;
        background-image: var(--bg-noise);
        opacity: 0.02;
        /* Subtle grain for physical feel */
        pointer-events: none;
        z-index: 9999;
      }

      /* V6 Refined Glass Token - Reduced Blur for realism */
      .glass-acrylic {
        background: rgba(255, 255, 255, 0.55);
        /* Lighter base */
        backdrop-filter: blur(12px) saturate(140%);
        -webkit-backdrop-filter: blur(12px) saturate(140%);
        border-radius: 20px;
        box-shadow: var(--shadow-glass);
        border: 1px solid rgba(255, 255, 255, 0.5);
        /* Physical rim light */
        border-top: 1px solid rgba(255, 255, 255, 0.9);
        border-left: 1px solid rgba(255, 255, 255, 0.9);
        position: relative;
        overflow: hidden;
        transition: transform var(--dur-hover) var(--ease-out-smooth), box-shadow var(--dur-hover) ease, backdrop-filter var(--dur-hover) ease;
      }

      .glass-acrylic::before {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        background-image: var(--bg-noise);
        opacity: 0.02;
        z-index: 0;
      }

      .glass-acrylic::after {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        z-index: 1;
        background: linear-gradient(110deg, transparent 20%, rgba(255, 255, 255, 0.5) 40%, transparent 60%);
        transform: translateX(-150%);
        animation: glassSweep 8s infinite var(--ease-in-out);
      }

      @keyframes glassSweep {

        0%,
        30% {
          transform: translateX(-150%);
        }

        70%,
        100% {
          transform: translateX(150%);
        }
      }

      @keyframes move-forever {
        0% {
          transform: translate(-2px, 0)
        }

        100% {
          transform: translate(0px, 0)
        }
      }

      .waves-use-el {
        animation: move-forever 15s linear infinite;
      }

      .waves-use-el:nth-child(2) {
        animation-duration: 20s;
        animation-delay: -5s;
      }

      .waves-use-el:nth-child(1) {
        animation-duration: 25s;
      }

      /* Premium Text Animations */
      @keyframes crystal-shimmer {
        0% {
          background-position: -200% center;
        }

        100% {
          background-position: 200% center;
        }
      }

      @keyframes text-bounce-up {
        0% {
          opacity: 0;
          transform: translateY(40px);
        }

        60% {
          opacity: 1;
          transform: translateY(-10px);
        }

        80% {
          transform: translateY(5px);
        }

        100% {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .animate-text-crystal {
        background: linear-gradient(to right,
            var(--c-navy) 20%,
            #405f99 30%,
            var(--c-navy) 70%,
            var(--c-navy) 80%);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        text-fill-color: transparent;
        background-size: 500% auto;
        animation: crystal-shimmer 4s linear infinite;
      }

      .animate-reveal-up-1,
      .animate-reveal-up-2,
      .animate-reveal-up-3 {
        opacity: 0;
      }

      .is-revealed.animate-reveal-up-1 {
        animation: text-bounce-up 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
      }

      .is-revealed.animate-reveal-up-2 {
        animation: text-bounce-up 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        animation-delay: 0.15s;
      }

      .is-revealed.animate-reveal-up-3 {
        animation: text-bounce-up 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        animation-delay: 0.3s;
      }

      /* High-End Electric Jagged Border */
      .electric-card-wrapper {
        position: relative;
        background: rgba(255, 255, 255, 0.45) !important;
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border: none !important;
        box-shadow: 0 8px 32px rgba(15, 23, 42, 0.04) !important;
      }

      .electric-card-wrapper::before,
      .electric-card-wrapper::after {
        content: '';
        position: absolute;
        inset: 0px;
        border-radius: inherit;
        border: 2px solid transparent;
        pointer-events: none;
        z-index: 10;
        transition: opacity 0.3s ease;
      }

      .electric-card-wrapper::before {
        border-color: #FFB800;
        /* Yellow */
        filter: url(#lightning-filter) drop-shadow(0 0 5px #FFB800) drop-shadow(0 0 10px #FFB800);
        opacity: 0.8;
      }

      .electric-card-wrapper::after {
        border-color: #ffffff;
        filter: url(#lightning-filter) drop-shadow(0 0 2px #ffffff);
        opacity: 0.9;
      }

      .electric-card-wrapper:hover::before {
        opacity: 1;
        filter: url(#lightning-filter) drop-shadow(0 0 8px #FFB800) drop-shadow(0 0 15px rgba(255, 184, 0, 0.8));
      }

      .electric-card-wrapper:hover::after {
        opacity: 1;
        filter: url(#lightning-filter) drop-shadow(0 0 4px #ffffff);
      }

      .electric-card-wrapper .text-h3,
      .electric-card-wrapper .text-body {
        color: var(--c-navy) !important;
      }

      .electric-card-wrapper .pc-list-item {
        color: var(--c-navy) !important;
      }

      .electric-card-wrapper .pc-list-item i {
        color: var(--c-slate-light) !important;
      }

      .electric-card-wrapper:hover .pc-list-item i {
        color: var(--c-navy) !important;
      }

      /* Typography Hierarchy */
      .text-display {
        font-size: clamp(3.5rem, 6.5vw, 6.2rem);
        line-height: 1.05;
        letter-spacing: -0.04em;
        font-weight: 800;
        color: var(--c-navy);
      }

      .text-h1 {
        font-size: clamp(2.5rem, 4vw, 3.5rem);
        line-height: 1.1;
        letter-spacing: -0.03em;
        font-weight: 700;
        color: var(--c-navy);
      }

      .text-h2 {
        font-size: clamp(2rem, 3.5vw, 2.75rem);
        line-height: 1.15;
        letter-spacing: -0.02em;
        font-weight: 700;
        color: var(--c-navy);
      }

      .text-h3 {
        font-size: 1.35rem;
        line-height: 1.4;
        font-weight: 600;
        color: var(--c-navy);
        letter-spacing: -0.01em;
      }

      .text-lead {
        font-size: 1.25rem;
        line-height: 1.6;
        color: var(--c-slate);
        font-weight: 400;
        max-width: 58ch;
      }

      .text-body {
        font-size: 1.05rem;
        line-height: 1.65;
        color: var(--c-slate);
        max-width: 60ch;
      }

      .text-small {
        font-size: 0.875rem;
        color: var(--c-slate-light);
        font-weight: 500;
      }

      .text-eyebrow {
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--c-slate-light);
        margin-bottom: 1rem;
        display: block;
      }

      /* Gradient Text Accents */
      .accent-gradient-indigo {
        background: linear-gradient(135deg, var(--c-navy), var(--c-indigo));
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      /* Layout */
      .section {
        padding: var(--space-section-ds) 0;
        position: relative;
      }

      .mb-large {
        margin-bottom: 3rem;
      }

      .mb-medium {
        margin-bottom: 1.5rem;
      }

      /* V6 Color Alternations */
      .b-alt {
        background: var(--c-cream-alt);
      }

      .b-white {
        background: var(--c-cream);
      }

      .b-tint-indigo {
        background: var(--c-tint-indigo);
      }

      .b-tint-lavender {
        background: var(--c-tint-lavender);
      }

      /* V6 Subtle Edges (removed hard black borders) */
      .edge-top {
        border-top: 1px solid rgba(15, 23, 42, 0.03);
      }

      .edge-bot {
        border-bottom: 1px solid rgba(15, 23, 42, 0.03);
      }

      .container {
        max-width: 1280px;
        margin: 0 auto;
        padding: 0 2rem;
        position: relative;
        z-index: 2;
      }

      .text-center {
        text-align: center;
      }

      .relative {
        position: relative;
      }

      /* Buttons: V6 Gradient & Pulse */
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1.75rem;
        font-size: 0.95rem;
        font-weight: 600;
        border-radius: 8px;
        text-decoration: none;
        border: 1px solid transparent;
        cursor: pointer;
        transition: all var(--dur-hover) var(--ease-out-smooth);
      }

      /* Buttons updated to classic yellow gradient */
      .btn-primary {
        background: linear-gradient(135deg, #FFB800, #E5A700);
        color: #1A1A2E;
        /* Dark text on yellow for high contrast */
        border: none;
        padding: 0.85rem 1.75rem;
        border-radius: 8px;
        font-weight: 700;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: all var(--dur-hover) var(--ease-out-smooth);
        box-shadow: 0 4px 15px rgba(255, 184, 0, 0.3);
        position: relative;
        overflow: hidden;
      }

      .btn-primary::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 50%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
        transition: left 0.6s ease;
        pointer-events: none;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(255, 184, 0, 0.5);
        background: linear-gradient(135deg, #E5A700, #FFB800);
      }

      .btn-primary:hover::before {
        left: 150%;
      }

      .btn-primary:active {
        transform: scale(0.97);
        box-shadow: 0 2px 8px rgba(255, 184, 0, 0.2);
      }

      .btn-outline {
        background: rgba(255, 255, 255, 0.5);
        /* Tinted transparent */
        color: var(--c-navy);
        border: 1px solid rgba(15, 23, 42, 0.1);
        backdrop-filter: blur(4px);
      }

      .btn-outline:hover {
        background: var(--c-base);
        border-color: rgba(15, 23, 42, 0.2);
        box-shadow: var(--shadow-micro);
      }

      /* Ambient Motion Glows */
      @keyframes floatGlow {
        0% {
          transform: scale(1) translate(0, 0);
          opacity: 0.6;
        }

        50% {
          transform: scale(1.1) translate(2%, 2%);
          opacity: 0.8;
=======
    <div class="page-transition overflow-hidden">
      <!-- Hero Section -->
      <section
        class="relative min-h-screen flex items-center pt-24 pb-16 bg-gradient-to-br from-white via-blue-50 to-indigo-50">
        <!-- Floating Elements -->
        <div
          class="absolute top-20 left-10 w-32 h-32 bg-yellow-200 rounded-full mix-blend-multiply filter blur-xl opacity-30 animate-pulse">
        </div>
        <div
          class="absolute bottom-20 right-10 w-48 h-48 bg-blue-200 rounded-full mix-blend-multiply filter blur-xl opacity-30 animate-pulse"
          style="animation-delay: 2s;"></div>

        <div class="container mx-auto px-6 relative z-10">
          <div class="flex flex-col lg:flex-row items-center gap-16">
            <!-- Hero Content -->
            <div class="lg:w-1/2 text-center lg:text-left">
              <div
                class="inline-flex items-center px-4 py-2 rounded-full bg-blue-50 text-blue-700 font-bold text-sm mb-6 animate-fade-in shadow-sm border border-blue-100">
                <img src="/img/custom/logo.png" alt="Logo" class="h-6 w-auto mr-3">
                The Future of Career Guidance is Here
              </div>
              <h1 class="text-5xl lg:text-7xl font-extrabold text-[#1A1A2E] leading-tight mb-8 animate-slide-in-left">
                Navigate Your <br />
                <span class="text-gradient-primary">Dream Career</span> with AI
              </h1>
              <p class="text-xl text-[#64748b] leading-relaxed mb-10 max-w-xl animate-fade-in"
                style="animation-delay: 0.2s;">
                Discover personalized paths, predict your perfect college, and master skills with our AI-powered
                counseling ecosystem.
              </p>
              <div class="flex flex-wrap justify-center lg:justify-start gap-6 animate-fade-in"
                style="animation-delay: 0.4s;">
                <a href="/cet-predictor" class="btn-primary px-8 py-4 text-lg">
                  <i class="fas fa-compass mr-2"></i> Predict College
                </a>
                <a href="/smart-quiz" class="btn-outline px-8 py-4 text-lg border-2">
                  <i class="fas fa-brain mr-2"></i> Take Career Quiz
                </a>
              </div>

              <!-- Trust Indicators -->
              <div class="mt-12 flex items-center justify-center lg:justify-start gap-8 opacity-70">
                <div class="flex flex-col">
                  <span class="text-2xl font-bold">50k+</span>
                  <span class="text-sm">Students Guided</span>
                </div>
                <div class="w-px h-10 bg-gray-300"></div>
                <div class="flex flex-col">
                  <span class="text-2xl font-bold">98%</span>
                  <span class="text-sm">Accuracy Rate</span>
                </div>
                <div class="w-px h-10 bg-gray-300"></div>
                <div class="flex flex-col">
                  <span class="text-2xl font-bold">150+</span>
                  <span class="text-sm">Career Paths</span>
                </div>
              </div>
            </div>

            <!-- Hero Visual -->
            <div class="lg:w-1/2 relative h-[500px] lg:h-[650px] flex items-center justify-center animate-float">
              <img src="/img/custom/hero_main.png" alt="EduNavigator AI"
                class="max-w-full max-h-full object-contain drop-shadow-2xl z-20">
              <!-- Decorative backplates -->
              <div
                class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[120%] bg-white/40 backdrop-blur-3xl rounded-full z-0">
              </div>
              <div
                class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[100%] h-[100%] border-2 border-blue-200/50 rounded-full animate-spin-slow z-10">
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Why Section -->
      <section class="py-24 bg-white relative overflow-hidden">
        <div class="container mx-auto px-6">
          <div class="flex flex-col lg:flex-row-reverse items-center gap-20">
            <!-- Content -->
            <div class="lg:w-1/2">
              <div class="text-[#FFB800] font-bold tracking-widest uppercase text-sm mb-4">Transforming Education</div>
              <h2 class="text-4xl lg:text-5xl font-bold text-[#1A1A2E] mb-8 leading-tight">
                Why Choose AI Career Counseling?
              </h2>
              <p class="text-lg text-[#64748b] mb-10 leading-relaxed">
                Traditional methods are based on old data. Our AI analyzes <strong>trillion-point market trends</strong>
                and <strong>psychometric markers</strong> to find where you truly belong.
              </p>

              <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="flex items-start gap-4 p-4 rounded-2xl hover:bg-amber-50 transition-colors duration-300">
                  <div
                    class="w-12 h-12 bg-amber-100 flex items-center justify-center rounded-xl text-amber-600 shrink-0">
                    <i class="fas fa-bolt text-xl"></i>
                  </div>
                  <div>
                    <h4 class="font-bold text-lg mb-1">Instant Insights</h4>
                    <p class="text-sm text-gray-500 font-medium">Get results in seconds, not weeks of expensive
                      sessions.</p>
                  </div>
                </div>
                <div class="flex items-start gap-4 p-4 rounded-2xl hover:bg-blue-50 transition-colors duration-300">
                  <div class="w-12 h-12 bg-blue-100 flex items-center justify-center rounded-xl text-blue-600 shrink-0">
                    <i class="fas fa-fingerprint text-xl"></i>
                  </div>
                  <div>
                    <h4 class="font-bold text-lg mb-1">Truly Personalized</h4>
                    <p class="text-sm text-gray-500 font-medium">Mapped to your unique strengths and personality.</p>
                  </div>
                </div>
                <div class="flex items-start gap-4 p-4 rounded-2xl hover:bg-indigo-50 transition-colors duration-300">
                  <div
                    class="w-12 h-12 bg-indigo-100 flex items-center justify-center rounded-xl text-indigo-600 shrink-0">
                    <i class="fas fa-globe text-xl"></i>
                  </div>
                  <div>
                    <h4 class="font-bold text-lg mb-1">Global Coverage</h4>
                    <p class="text-sm text-gray-500 font-medium">Access data from colleges and careers worldwide.</p>
                  </div>
                </div>
                <div class="flex items-start gap-4 p-4 rounded-2xl hover:bg-emerald-50 transition-colors duration-300">
                  <div
                    class="w-12 h-12 bg-emerald-100 flex items-center justify-center rounded-xl text-emerald-600 shrink-0">
                    <i class="fas fa-shield-check text-xl"></i>
                  </div>
                  <div>
                    <h4 class="font-bold text-lg mb-1">Evidence Based</h4>
                    <p class="text-sm text-gray-500 font-medium">Scientific accuracy powered by data analytics.</p>
                  </div>
                </div>
              </div>
            </div>
            <!-- Visual -->
            <div class="lg:w-1/2">
              <div class="relative p-8 group">
                <div
                  class="absolute inset-0 bg-blue-600 rounded-[3rem] rotate-3 group-hover:rotate-0 transition-transform duration-500">
                </div>
                <img src="/img/custom/why_choose.png" alt="Collaborative Learning"
                  class="relative z-10 rounded-[2.5rem] shadow-2xl transition-transform duration-500 group-hover:-translate-y-4">
                <!-- Floating Toast -->
                <div
                  class="absolute -bottom-6 -right-6 lg:-right-12 z-20 bg-white p-6 rounded-2xl shadow-2xl animate-float">
                  <div class="flex items-center gap-4 mb-2">
                    <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center text-green-600">
                      <i class="fas fa-check"></i>
                    </div>
                    <div>
                      <div class="font-bold">Career Match</div>
                      <div class="text-xs text-gray-400">96.5% Probability</div>
                    </div>
                  </div>
                  <div class="text-sm font-semibold text-blue-600">Software Architect</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Services Bento Grid -->
      <section class="py-24 bg-gray-50">
        <div class="container mx-auto px-6">
          <div class="text-center max-w-3xl mx-auto mb-20">
            <h2 class="text-4xl lg:text-5xl font-bold text-[#1A1A2E] mb-6">Our Intelligence Suite</h2>
            <p class="text-lg text-gray-500">Comprehensive tools designed to take you from student to professional.</p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            <!-- AI Search -->
            <a href="/career-bank" class="premium-card group hover:bg-[#1A1A2E]">
              <div
                class="w-14 h-14 bg-amber-100 rounded-2xl flex items-center justify-center text-amber-600 mb-8 transition-colors group-hover:bg-amber-500 group-hover:text-white">
                <i class="fas fa-database text-2xl"></i>
              </div>
              <h3 class="text-2xl font-bold mb-4 group-hover:text-white">Career Bank</h3>
              <p class="text-gray-500 group-hover:text-gray-300 mb-6">Explore 500+ career paths with deep insights into
                salary, required skills, and growth.</p>
              <span class="text-blue-600 font-bold group-hover:text-amber-400 transition-colors">Learn More <i
                  class="fas fa-arrow-right ml-2 group-hover:translate-x-2 transition-transform"></i></span>
            </a>

            <!-- College Finder -->
            <a href="/exam_choice" class="premium-card group hover:bg-[#1A1A2E] md:row-span-2">
              <div
                class="w-14 h-14 bg-indigo-100 rounded-2xl flex items-center justify-center text-indigo-600 mb-8 transition-colors group-hover:bg-indigo-500 group-hover:text-white">
                <i class="fas fa-university text-2xl"></i>
              </div>
              <h3 class="text-2xl font-bold mb-4 group-hover:text-white text-balance">College Predictor</h3>
              <p class="text-gray-500 group-hover:text-gray-300 mb-10 leading-relaxed">Our advanced algorithm predicts
                which top colleges fit your marks and preferences with high accuracy.</p>
              <div class="mt-auto bg-gray-100 group-hover:bg-white/10 p-4 rounded-xl mb-6">
                <div class="text-xs text-gray-400 uppercase font-bold mb-2">Supported Exams</div>
                <div class="flex flex-wrap gap-2">
                  <span
                    class="px-2 py-1 bg-white group-hover:bg-indigo-600 group-hover:text-white rounded text-[10px] font-bold">JEE
                    MAINS</span>
                  <span
                    class="px-2 py-1 bg-white group-hover:bg-indigo-600 group-hover:text-white rounded text-[10px] font-bold">MHT-CET</span>
                </div>
              </div>
              <span class="text-blue-600 font-bold group-hover:text-indigo-400 transition-colors">Identify Best Match <i
                  class="fas fa-arrow-right ml-2 group-hover:translate-x-2 transition-transform"></i></span>
            </a>

            <!-- AI Interview -->
            <div class="premium-card relative overflow-hidden group hover:bg-blue-600 md:col-span-2">
              <div class="flex flex-col md:flex-row items-center gap-10">
                <div class="w-full md:w-3/5">
                  <div
                    class="inline-block px-3 py-1 bg-blue-100 text-blue-700 text-xs font-bold rounded-full mb-4 group-hover:bg-white/20 group-hover:text-white">
                    FEATURED</div>
                  <h3 class="text-3xl font-bold mb-4 group-hover:text-white">AI Interview Simulator</h3>
                  <p class="text-gray-500 group-hover:text-blue-100 text-lg mb-8 leading-relaxed">Practice your dream
                    job interview with our conversational AI. Get real-time feedback on your speech, confidence, and
                    answers.</p>
                  <a href="/interview-simulator" class="btn-primary group-hover:bg-white group-hover:text-blue-600">Try
                    Simulation Now</a>
                </div>
                <div class="w-full md:w-2/5 shrink-0">
                  <img src="/img/custom/ai_interview_icon.png" alt="AI Interview"
                    class="w-full animate-float opacity-90">
                </div>
              </div>
            </div>

            <!-- Aptitude -->
            <a href="/apti" class="premium-card group hover:bg-[#1A1A2E]">
              <div
                class="w-14 h-14 bg-emerald-100 rounded-2xl flex items-center justify-center text-emerald-600 mb-8 transition-colors group-hover:bg-emerald-500 group-hover:text-white">
                <i class="fas fa-brain text-2xl"></i>
              </div>
              <h3 class="text-2xl font-bold mb-4 group-hover:text-white">Aptitude Test</h3>
              <p class="text-gray-500 group-hover:text-gray-300 mb-6">Scientific assessments that measure your cognitive
                abilities and natural flair.</p>
              <span class="text-blue-600 font-bold group-hover:text-emerald-400 transition-colors">Start Assessment <i
                  class="fas fa-arrow-right ml-2 group-hover:translate-x-2 transition-transform"></i></span>
            </a>

            <!-- Roadmap -->
            <a href="/career-roadmap" class="premium-card group hover:bg-[#1A1A2E]">
              <div class="flex justify-between items-start mb-4">
                <div
                  class="w-14 h-14 bg-purple-100 rounded-2xl flex items-center justify-center text-purple-600 transition-colors group-hover:bg-purple-500 group-hover:text-white">
                  <i class="fas fa-route text-2xl"></i>
                </div>
                <img src="/img/custom/career_roadmap_icon.png" alt="Roadmap Icon"
                  class="w-16 h-16 group-hover:scale-110 transition-transform duration-500">
              </div>
              <h3 class="text-2xl font-bold mb-4 group-hover:text-white">Career Roadmap</h3>
              <p class="text-gray-500 group-hover:text-gray-300 mb-6">Step-by-step navigational guide from where you are
                to your ultimate goal.</p>
              <span class="text-blue-600 font-bold group-hover:text-purple-400 transition-colors mt-auto">View My Path
                <i class="fas fa-arrow-right ml-2 group-hover:translate-x-2 transition-transform"></i></span>
            </a>

            <!-- Resources -->
            <a href="/scholarship" class="premium-card group hover:bg-[#1A1A2E]">
              <div
                class="w-14 h-14 bg-rose-100 rounded-2xl flex items-center justify-center text-rose-600 mb-8 transition-colors group-hover:bg-rose-500 group-hover:text-white">
                <i class="fas fa-graduation-cap text-2xl"></i>
              </div>
              <h3 class="text-2xl font-bold mb-4 group-hover:text-white">Scholarships</h3>
              <p class="text-gray-500 group-hover:text-gray-300 mb-6">Funding your education shouldn't be hard. Find
                grants you qualify for easily.</p>
              <span class="text-blue-600 font-bold group-hover:text-rose-400 transition-colors">Explore Grants <i
                  class="fas fa-arrow-right ml-2 group-hover:translate-x-2 transition-transform"></i></span>
            </a>
          </div>
        </div>
      </section>

      <!-- Performance Spotlight -->
      <section class="py-24 bg-white">
        <div class="container mx-auto px-6">
          <div class="flex flex-col lg:flex-row items-center gap-16">
            <div class="lg:w-1/2">
              <h2 class="text-4xl font-bold text-[#1A1A2E] mb-8 leading-tight">Advanced Performance <br />Analytics &
                Leaderboards</h2>
              <p class="text-lg text-gray-500 mb-10 leading-relaxed">Don't just practice—compete and improve. Our
                dashboard tracks your progress over time, identifying micro-weaknesses and celebrating your milestones
                on the global stage.</p>

              <ul class="space-y-6">
                <li class="flex items-center gap-4">
                  <div class="w-6 h-6 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 text-xs">
                    <i class="fas fa-check"></i>
                  </div>
                  <span class="font-semibold text-gray-700">Real-time Rank Updates</span>
                </li>
                <li class="flex items-center gap-4">
                  <div class="w-6 h-6 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 text-xs">
                    <i class="fas fa-check"></i>
                  </div>
                  <span class="font-semibold text-gray-700">Skill-based Weakness Detection</span>
                </li>
                <li class="flex items-center gap-4">
                  <div class="w-6 h-6 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 text-xs">
                    <i class="fas fa-check"></i>
                  </div>
                  <span class="font-semibold text-gray-700">Badge & Achievement System</span>
                </li>
              </ul>

              <div class="mt-12">
                <a href="/leaderboard" class="btn-primary hover:bg-[#1A1A2E] hover:text-white">
                  <i class="fas fa-medal mr-2"></i> View Leaderboard
                </a>
              </div>
            </div>

            <div class="lg:w-1/2 relative">
              <!-- Dashboard Visual Card -->
              <div class="bg-[#1A1A2E] p-8 rounded-[2rem] shadow-2xl overflow-hidden relative group">
                <!-- Glowing spots -->
                <div class="absolute -top-10 -right-10 w-40 h-40 bg-blue-500/20 blur-[80px]"></div>
                <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-purple-500/20 blur-[80px]"></div>

                <div class="flex justify-between items-center mb-10 border-b border-white/10 pb-6">
                  <div class="flex items-center gap-3">
                    <div class="w-10 h-10 rounded-full bg-white/10 flex items-center justify-center text-yellow-400">
                      <i class="fas fa-user"></i>
                    </div>
                    <div>
                      <div class="text-white font-bold text-sm">Alex Johnson</div>
                      <div class="text-blue-400 text-xs">Rank #1 (Engineering)</div>
                    </div>
                  </div>
                  <div class="text-right">
                    <div class="text-gray-400 text-[10px] uppercase font-black">Score</div>
                    <div class="text-2xl font-black text-white">982</div>
                  </div>
                </div>

                <!-- Graph Placeholder -->
                <div class="h-48 mb-8 relative flex items-end justify-between px-4">
                  <div class="w-8 bg-blue-500/40 rounded-t-lg transition-all duration-500 group-hover:h-32 h-16"></div>
                  <div class="w-8 bg-blue-500/60 rounded-t-lg transition-all duration-500 group-hover:h-40 h-24"></div>
                  <div class="w-8 bg-amber-500 rounded-t-lg transition-all duration-500 group-hover:h-44 h-32"></div>
                  <div class="w-8 bg-blue-500/80 rounded-t-lg transition-all duration-500 group-hover:h-28 h-12"></div>
                  <div class="w-8 bg-blue-500 rounded-t-lg transition-all duration-500 group-hover:h-36 h-20"></div>
                </div>

                <!-- Mini Score Cards -->
                <div class="grid grid-cols-2 gap-4">
                  <div class="bg-white/5 p-4 rounded-xl border border-white/5">
                    <div class="text-[10px] text-gray-400 uppercase font-black mb-1 text-center">Logic</div>
                    <div class="text-center font-bold text-white">85%</div>
                  </div>
                  <div class="bg-white/5 p-4 rounded-xl border border-white/5">
                    <div class="text-[10px] text-gray-400 uppercase font-black mb-1 text-center">Quant</div>
                    <div class="text-center font-bold text-amber-500">62%</div>
                  </div>
                </div>

                <!-- Toast -->
                <div
                  class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 bg-white/10 backdrop-blur-md border border-white/20 px-6 py-3 rounded-full text-white text-sm font-bold shadow-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <i class="fas fa-chart-line mr-2 text-green-400"></i> New Personal Best!
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Testimonials Marquee -->
      <section class="py-24 bg-gray-50 border-y border-gray-200 overflow-hidden">
        <div class="container mx-auto px-6 text-center mb-16">
          <h2 class="text-4xl font-bold text-[#1A1A2E]">Voice of our Students</h2>
          <p class="text-gray-500 mt-4">Real stories from the EduNavigator community.</p>
        </div>

        <div class="marquee-wrapper relative">
          <!-- Left Gradient -->
          <div class="absolute left-0 top-0 w-40 h-full bg-gradient-to-r from-gray-50 to-transparent z-10"></div>
          <!-- Right Gradient -->
          <div class="absolute right-0 top-0 w-40 h-full bg-gradient-to-l from-gray-50 to-transparent z-10"></div>

          <div class="marquee-track flex gap-8 py-4">
            <!-- Testimonial Item (Duplicate for infinite effect) -->
            <% const testimonials=[ { name: 'Aarav Patel' , role: 'Computer Science' ,
              text: 'The AI career roadmap helped me visualize my path clearly. Highly recommended!' , color: 'blue' },
              { name: 'Zara Khan' , role: 'Psychology Aspirant' ,
              text: 'I was confused about my options, but the aptitude test nailed my strengths.' , color: 'purple' }, {
              name: 'Rohan Gupta' , role: 'Mechanical Eng.' ,
              text: 'Found the best college for my rank using the Finder tool. Saved weeks of stress.' , color: 'amber'
              }, { name: 'Ishita Sharma' , role: 'Medical Aspirant' ,
              text: 'The scholarship database is a goldmine. I found a grant I didn\' t know existed.', color: 'emerald'
              }, { name: 'Vivaan Singh' , role: 'Economics Student' ,
              text: 'The webinars gave me real insights into what an economist actually does.' , color: 'rose' } ]; %>

              <% [...testimonials, ...testimonials, ...testimonials].forEach((t, i)=> { %>
                <div
                  class="min-w-[350px] bg-white p-8 rounded-3xl shadow-sm border border-gray-100 hover:shadow-xl transition-all duration-300">
                  <div class="flex items-center gap-4 mb-6">
                    <div
                      class="w-12 h-12 rounded-full bg-<%= t.color %>-100 text-<%= t.color %>-600 flex items-center justify-center font-black text-xl">
                      <%= t.name[0] %>
                    </div>
                    <div>
                      <div class="font-bold text-[#1A1A2E]">
                        <%= t.name %>
                      </div>
                      <div class="text-xs text-gray-400 font-bold uppercase">
                        <%= t.role %>
                      </div>
                    </div>
                  </div>
                  <p class="text-gray-600 italic leading-relaxed">"<%= t.text %>"</p>
                  <div class="mt-6 flex text-yellow-400 text-xs gap-1">
                    <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i
                      class="fas fa-star"></i><i class="fas fa-star"></i>
                  </div>
                </div>
                <% }); %>
          </div>
        </div>
      </section>

      <!-- Final CTA -->
      <section class="py-24 bg-[#1A1A2E] relative overflow-hidden">
        <div class="absolute top-0 right-0 w-[500px] h-[500px] bg-blue-600/10 rounded-full blur-[100px]"></div>
        <div class="absolute bottom-0 left-0 w-[500px] h-[500px] bg-amber-600/10 rounded-full blur-[100px]"></div>

        <div class="container mx-auto px-6 relative z-10">
          <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-5xl lg:text-7xl font-bold text-white mb-8 leading-tight">
              Ready to build your <br />
              <span class="text-[#FFB800]">Dream Career?</span>
            </h2>
            <p class="text-xl text-blue-100/60 mb-12 max-w-2xl mx-auto">
              Join thousands of students who are already navigating their future with confidence. Signing up takes less
              than a minute.
            </p>
            <div class="flex flex-wrap justify-center gap-6">
              <a href="/register" class="btn-primary px-10 py-5 text-xl bg-[#FFB800] hover:bg-amber-400">
                Create Free Account
              </a>
              <a href="/login" class="btn-glass px-10 py-5 text-xl border-white/20">
                Welcome Back
              </a>
            </div>
          </div>
        </div>
      </section>
    </div>

    <style>
      /* Custom Overrides for Home Page */
      :root {
        --color-primary: #FFB800;
        --color-secondary: #00A8FF;
      }

      .text-gradient-primary {
        background: linear-gradient(135deg, #FFB800 0%, #FF9F00 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .btn-primary {
        transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      }

      .animate-float {
        animation: float 6s ease-in-out infinite;
      }

      @keyframes float {

        0%,
        100% {
          transform: translateY(0px);
        }

        50% {
          transform: translateY(-20px);
        }
      }

      @keyframes spin-slow {
        from {
          transform: translate(-50%, -50%) rotate(0deg);
        }

        to {
          transform: translate(-50%, -50%) rotate(360deg);
>>>>>>> origin/Teachers-students-connect
        }
      }

<<<<<<< HEAD
        100% {
          transform: scale(1) translate(0, 0);
          opacity: 0.6;
        }
      }

      /* ================== V5/V6: 1. HERO & PLATFORM OVERVIEW ================== */
      .hero {
        min-height: 100vh;
        padding-top: 180px;
        padding-bottom: 80px;
        display: flex;
        align-items: center;
        position: relative;
        background: var(--c-cream-alt);
        overflow: hidden;
      }

      /* V6 Hero Ambient Indigo Glow */
      .hero::before {
        content: '';
        position: absolute;
        top: 10%;
        right: 5%;
        width: 50vw;
        height: 50vw;
        background: radial-gradient(circle, var(--c-indigo-glow) 0%, transparent 70%);
        filter: blur(100px);
        z-index: 0;
        pointer-events: none;
        animation: floatGlow 15s ease-in-out infinite;
      }

      .hero-grid {
        display: grid;
        grid-template-columns: 10fr 12fr;
        gap: var(--gap-lg);
        align-items: center;
      }

      .hero-content {
        max-width: 500px;
      }

      .hero-pillars {
        display: flex;
        gap: var(--gap-md);
        margin-top: 2.5rem;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(15, 23, 42, 0.05);
      }

      .hp-item {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.85rem;
        font-weight: 600;
        color: var(--c-slate);
      }

      .hp-item i {
        font-size: 1rem;
        color: var(--c-indigo);
        opacity: 0.8;
      }

      .hero-visual {
        position: relative;
        height: 500px;
        width: 100%;
        perspective: 1200px;
      }

      /* Base glass retains its properties */
      .mockup-base {
        position: absolute;
        width: 520px;
        height: 380px;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%) rotateY(-5deg) rotateX(2deg);
        padding: 0;
        display: flex;
        flex-direction: column;
        z-index: 2;
        animation: mockFloat var(--dur-float) ease-in-out infinite alternate;
      }

      @keyframes mockFloat {
        0% {
          transform: translate(-50%, -50%) rotateY(-5deg) rotateX(2deg) translateY(0);
        }

        100% {
          transform: translate(-50%, -50%) rotateY(-5deg) rotateX(2deg) translateY(-15px);
        }
      }

      .mock-nav {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(15, 23, 42, 0.05);
        display: flex;
        align-items: center;
        justify-content: space-between;
        z-index: 3;
        position: relative;
      }

      .mock-dots {
        display: flex;
        gap: 6px;
      }

      .mock-dots span {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.1);
      }

      .mock-dots span:nth-child(1) {
        background: rgba(15, 23, 42, 0.2);
      }

      .mock-body {
        padding: 2rem;
        flex: 1;
        position: relative;
        z-index: 3;
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      .mock-skeleton {
        height: 8px;
        border-radius: 4px;
        background: rgba(15, 23, 42, 0.04);
        position: relative;
        overflow: hidden;
      }

      .mock-skeleton::after {
        content: '';
        position: absolute;
        inset: 0;
        transform: translateX(-100%);
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
        animation: shimmer 2.5s infinite;
      }

      @keyframes shimmer {
        100% {
          transform: translateX(100%);
        }
      }

      /* V6 Refined Cards: Tinted off-white, physical rim light, soft shadow */
      .mock-internal-card {
        background: var(--c-base);
        /* Just barely off-white */
        padding: 1rem;
        border-radius: 12px;
        border: 1px solid rgba(15, 23, 42, 0.03);
        border-top: 1px solid rgba(255, 255, 255, 0.8);
        border-left: 1px solid rgba(255, 255, 255, 0.8);
        margin-top: 1rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        position: relative;
        box-shadow: var(--shadow-soft);
      }

      /* ================== V5/V6: 2. CORE PILLARS (THE MAIN HUB) ================== */
      .hub-section {
        background: var(--c-cream);
        position: relative;
        overflow: hidden;
      }

      /* V6 Hub Ambient Teal Glow */
      .hub-section::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: -10%;
        width: 60vw;
        height: 60vw;
        background: radial-gradient(circle, var(--c-teal-glow) 0%, transparent 60%);
        filter: blur(120px);
        z-index: 0;
        pointer-events: none;
      }

      .pillar-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--gap-lg);
        margin-top: 4rem;
        position: relative;
        z-index: 1;
      }

      /* V10 Elevated SaaS Glass Cards */
      .pillar-card {
        background: rgba(255, 255, 255, 0.45);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border-radius: 24px;
        padding: var(--card-pad);
        border: 1px solid rgba(255, 255, 255, 0.6);
        box-shadow: 0 8px 32px rgba(15, 23, 42, 0.04);
        position: relative;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        --mouse-x: 50%;
        --mouse-y: 50%;
        transform-style: preserve-3d;

        /* Pre-reveal state */
        opacity: 0;
        transform: translateY(40px);
        transition: opacity 0.8s ease, transform 0.8s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.3s ease;
      }

      .pillar-card.revealed {
        opacity: 1;
        transform: translateY(0);
      }

      .pillar-card.revealed:hover {
        transform: translateY(-4px) perspective(1000px) rotateX(var(--rotate-x, 0deg)) rotateY(var(--rotate-y, 0deg));
        box-shadow: 0 24px 48px rgba(15, 23, 42, 0.08), 0 4px 12px rgba(15, 23, 42, 0.04);
        /* Fast transition for smooth mouse tracking */
        transition: transform 0.1s ease-out, box-shadow 0.3s ease;
      }

      /* Inner Hover Glow */
      .pillar-card::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(600px circle at var(--mouse-x) var(--mouse-y),
            rgba(255, 255, 255, 0.8),
            transparent 40%);
        opacity: 0;
        transition: opacity 0.3s ease;
        z-index: 0;
        pointer-events: none;
      }

      /* Shine Border Effect using pseudo-element */
      .pillar-card::after {
        content: '';
        position: absolute;
        inset: 0;
        border-radius: inherit;
        padding: 1px;
        background: radial-gradient(400px circle at var(--mouse-x) var(--mouse-y),
            rgba(255, 255, 255, 1),
            transparent 40%);
        -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        opacity: 0;
        transition: opacity 0.3s ease;
        pointer-events: none;
        z-index: 2;
      }

      .pillar-card:hover::before,
      .pillar-card:hover::after {
        opacity: 1;
      }

      .pillar-card>* {
        position: relative;
        z-index: 1;
      }

      /* Spot Colors for Hierarchy */
      .pc-features .pc-icon-wrap {
        background: linear-gradient(135deg, var(--c-indigo-soft), rgba(255, 184, 0, 0.2));
        color: var(--c-indigo);
        border: 1px solid rgba(255, 184, 0, 0.3);
        box-shadow: 0 4px 16px var(--c-indigo-glow);
      }

      .pc-tools .pc-icon-wrap {
        background: linear-gradient(135deg, var(--c-teal-soft), rgba(0, 168, 255, 0.2));
        color: var(--c-teal);
        border: 1px solid rgba(0, 168, 255, 0.3);
        box-shadow: 0 4px 16px var(--c-teal-glow);
      }

      .pc-assess .pc-icon-wrap {
        background: linear-gradient(135deg, var(--c-amber-soft), rgba(108, 99, 255, 0.2));
        color: var(--c-amber);
        border: 1px solid rgba(108, 99, 255, 0.3);
        box-shadow: 0 4px 16px var(--c-amber-glow);
      }

      .pc-resources .pc-icon-wrap {
        background: linear-gradient(135deg, var(--c-coral-soft), rgba(16, 185, 129, 0.2));
        color: var(--c-coral);
        border: 1px solid rgba(16, 185, 129, 0.3);
        box-shadow: 0 4px 16px var(--c-coral-glow);
      }

      .pc-header {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
      }

      .pc-icon-wrap {
        width: 56px;
        height: 56px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        flex-shrink: 0;
        position: relative;
        overflow: hidden;
        transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      }

      .pillar-card:hover .pc-icon-wrap {
        transform: scale(1.1) rotate(5deg);
      }

      .pc-list {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-top: 1rem;
      }

      .pc-list-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        color: var(--c-navy);
        font-weight: 500;
        font-size: 0.95rem;
        transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      }

      .pillar-card:hover .pc-list-item {
        transform: translateX(6px);
      }

      .pc-list-item i {
        color: var(--c-slate-light);
        font-size: 0.8rem;
        transition: color 0.3s ease;
      }

      .pillar-card:hover .pc-list-item i {
        color: var(--c-navy);
      }

      /* Pillar Subtle Connectors */
      .hub-connectors {
        position: absolute;
        inset: 0;
        pointer-events: none;
        z-index: 0;
        opacity: 0.6;
      }

      .hub-connectors svg {
        width: 100%;
        height: 100%;
      }

      .animated-path {
        fill: none;
        stroke: url(#network-gradient);
        stroke-width: 1.5;
        stroke-dasharray: 12 12;
        animation: dashMove 30s linear infinite;
        opacity: 0.4;
      }

      @keyframes dashMove {
        from {
          stroke-dashoffset: 0;
        }

        to {
          stroke-dashoffset: -1000;
        }
      }

      /* ================== V5/V6: 3. FEATURE STORYTELLING ================== */
      .story-section {
        background: var(--c-tint-lavender);
        /* V6: Light lavender atmospheric layer */
        position: relative;
        overflow: hidden;
      }

      /* V6 Story Ambient Glow */
      .story-section::after {
        content: '';
        position: absolute;
        top: 20%;
        right: -10%;
        width: 70vw;
        height: 70vw;
        background: radial-gradient(circle, var(--c-indigo-glow) 0%, transparent 60%);
        filter: blur(120px);
        z-index: 0;
        pointer-events: none;
      }

      .story-block {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--gap-xl);
        align-items: center;
        margin-bottom: 8rem;
        position: relative;
        z-index: 1;
      }

      .story-block:last-child {
        margin-bottom: 0;
      }

      .story-block.reversed {
        direction: rtl;
      }

      .story-block.reversed>* {
        direction: ltr;
      }

      .story-text {
        max-width: 460px;
      }

      .story-pill {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 6px 12px;
        border-radius: 20px;
        background: var(--c-base);
        border: 1px solid rgba(15, 23, 42, 0.05);
        font-size: 0.8rem;
        font-weight: 600;
        color: var(--c-navy);
        margin-bottom: 1.5rem;
        box-shadow: var(--shadow-micro);
      }

      /* V6 Elevated Image Wrap */
      .story-visual-wrap {
        position: relative;
        width: 100%;
        aspect-ratio: 4/3;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--c-base);
        border-radius: 24px;
        box-shadow: var(--shadow-float);
        border: 1px solid rgba(15, 23, 42, 0.03);
        border-top: 1px solid rgba(255, 255, 255, 0.8);
        border-left: 1px solid rgba(255, 255, 255, 0.8);
        overflow: hidden;
      }

      .sv-bg-grid {
        position: absolute;
        inset: 0;
        background-image: radial-gradient(#E2E8F0 1px, transparent 1px);
        background-size: 20px 20px;
        opacity: 0.5;
      }

      /* Specific UI mocks for storytelling */
      .ui-mock-form {
        width: 80%;
        background: var(--c-base);
        border-radius: 12px;
        box-shadow: var(--shadow-base-ui);
        border: 1px solid rgba(15, 23, 42, 0.04);
        border-top: 1px solid rgba(255, 255, 255, 0.9);
        padding: 1.5rem;
        z-index: 2;
        position: relative;
      }

      .ui-skeleton-line {
        height: 12px;
        background: rgba(15, 23, 42, 0.04);
        border-radius: 4px;
        margin-bottom: 12px;
        width: 100%;
      }

      /* ================== V5/V6: 4. ECOSYSTEM JOURNEY ================== */
      .journey-section {
        background: var(--c-cream-alt);
        overflow: hidden;
        padding-bottom: 160px;
        position: relative;
      }

      /* Subtle Indigo glow behind the path */
      .journey-section::before {
        content: '';
        position: absolute;
        top: 10%;
        left: 50%;
        width: 80vw;
        height: 30vw;
        transform: translateX(-50%);
        background: radial-gradient(ellipse, var(--c-indigo-glow) 0%, transparent 70%);
        filter: blur(80px);
        pointer-events: none;
        z-index: 0;
      }

      .journey-flow {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        position: relative;
        margin-top: 4rem;
        padding-top: 2rem;
        z-index: 1;
      }

      .j-line {
        position: absolute;
        top: 22px;
        left: 0;
        width: 100%;
        height: 2px;
        background: linear-gradient(90deg, transparent, var(--c-indigo), transparent);
        opacity: 0.15;
        z-index: 0;
      }

      .j-node {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        position: relative;
        z-index: 1;
        width: 140px;
      }

      .j-dot {
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: var(--c-base);
        border: 2px solid var(--c-indigo-soft);
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: var(--shadow-soft);
        margin-bottom: 1rem;
        color: var(--c-indigo);
        font-size: 1.1rem;
        transition: all var(--dur-hover);
      }

      .j-node:hover .j-dot {
        transform: scale(1.1);
        border-color: var(--c-indigo);
        background: var(--c-indigo);
        color: white;
        box-shadow: var(--shadow-glow-indigo);
      }

      .j-title {
        font-weight: 700;
        font-size: 0.95rem;
        color: var(--c-navy);
        margin-bottom: 4px;
      }

      .j-desc {
        font-size: 0.8rem;
        color: var(--c-slate-light);
        line-height: 1.4;
      }

      /* ================== V5/V6: 5. GROWTH HUB (Analytics) ================== */
      .growth-section {
        background: var(--c-cream);
        position: relative;
        overflow: hidden;
      }

      /* V6 Amber Warm Glow */
      .growth-section::before {
        content: '';
        position: absolute;
        top: 30%;
        right: -5%;
        width: 60vw;
        height: 60vw;
        background: radial-gradient(circle, var(--c-amber-glow) 0%, transparent 60%);
        filter: blur(120px);
        z-index: 0;
        pointer-events: none;
      }

      .growth-wrap {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--gap-xl);
        align-items: center;
        position: relative;
        z-index: 1;
      }

      .growth-context {
        padding-right: 2rem;
        border-right: 1px solid rgba(15, 23, 42, 0.05);
      }

      .analytics-board {
        position: relative;
        width: 100%;
        aspect-ratio: 1;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .a-sheet {
        position: absolute;
        width: 100%;
        height: 90%;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 1;
      }

      /* V6 Elevated Main Chart Card */
      .a-card-main {
        position: relative;
        width: 70%;
        background: var(--c-base);
        border-radius: 18px;
        box-shadow: var(--shadow-base-ui);
        border: 1px solid rgba(15, 23, 42, 0.03);
        border-top: 1px solid rgba(255, 255, 255, 0.8);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 2.5rem 1.5rem;
        z-index: 3;
      }

      .a-ring-wrap {
        position: relative;
        width: 160px;
        height: 160px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
      }

      .a-ring-svg {
        position: absolute;
        inset: 0;
        transform: rotate(-90deg);
      }

      .a-ring-bg {
        fill: none;
        stroke: var(--c-cream-alt);
        stroke-width: 6;
      }

      .a-ring-fg {
        fill: none;
        stroke: var(--c-indigo);
        stroke-width: 6;
        stroke-dasharray: 440;
        stroke-dashoffset: 440;
        transition: stroke-dashoffset var(--dur-chart) var(--ease-in-out);
        stroke-linecap: round;
        animation: ringPulse 4s infinite alternate;
        /* V6 slow color motion on ring */
      }

      @keyframes ringPulse {
        0% {
          filter: drop-shadow(0 0 4px rgba(99, 102, 241, 0.2));
        }

        100% {
          filter: drop-shadow(0 0 12px rgba(99, 102, 241, 0.5));
        }
      }

      .a-score {
        font-size: 3.5rem;
        font-weight: 800;
        letter-spacing: -0.04em;
        color: var(--c-navy);
        line-height: 1;
      }

      .a-stat-pill {
        position: absolute;
        background: #FFF;
        border: 1px solid rgba(0, 0, 0, 0.04);
        box-shadow: var(--shadow-base-ui);
        border-radius: 8px;
        padding: 0.75rem 1rem;
        font-size: 0.85rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        top: 10%;
        right: -5%;
        color: var(--c-teal);
        z-index: 4;
        animation: solidFloat 5s ease-in-out infinite alternate;
      }

      @keyframes solidFloat {
        0% {
          transform: translateY(0);
        }

        100% {
          transform: translateY(-8px);
        }
      }

      /* ================== V5/V6: 6. COMMUNITY & LEADERBOARD ================== */
      .community-section {
        background: var(--c-cream-alt);
        position: relative;
        overflow: hidden;
      }

      /* V6 Coral Ambient Glow */
      .community-section::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: -10%;
        width: 50vw;
        height: 50vw;
        background: radial-gradient(circle, var(--c-coral-glow) 0%, transparent 60%);
        filter: blur(120px);
        z-index: 0;
        pointer-events: none;
      }

      .com-grid {
        display: grid;
        grid-template-columns: 10fr 14fr;
        gap: var(--gap-xl);
        align-items: center;
        position: relative;
        z-index: 1;
      }

      /* Leaderboard Glass Preview */
      .lb-preview {
        display: flex;
        flex-direction: column;
        gap: 8px;
        padding: 1.5rem;
      }

      /* V6 Refined Leaderboard Rows */
      .lb-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 16px;
        background: var(--c-base);
        /* Not pure white */
        border-radius: 8px;
        border: 1px solid rgba(15, 23, 42, 0.03);
        border-top: 1px solid rgba(255, 255, 255, 0.8);
        box-shadow: var(--shadow-micro);
        transition: transform var(--dur-hover);
      }

      .lb-row:hover {
        transform: translateX(4px);
        box-shadow: var(--shadow-soft);
      }

      .lb-user {
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .lb-avatar {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background: #E2E8F0;
      }

      .lb-score {
        font-weight: 700;
        color: var(--c-indigo);
      }

      /* Cinematic Testimonials within Community */
      .test-marquee {
        display: flex;
        width: 100%;
        padding: 2rem 0;
        margin-top: 4rem;
        position: relative;
        z-index: 1;
        mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
        -webkit-mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
      }

      .test-track {
        display: flex;
        gap: var(--gap-md);
        animation: scrollHoriz 40s linear infinite;
        width: max-content;
      }

      .test-track:hover {
        animation-play-state: paused;
      }

      @keyframes scrollHoriz {
=======
      .animate-spin-slow {
        animation: spin-slow 20s linear infinite;
      }

      .marquee-track {
        animation: scroll 40s linear infinite;
        width: fit-content;
      }

      .marquee-track:hover {
        animation-play-state: paused;
      }

      @keyframes scroll {
>>>>>>> origin/Teachers-students-connect
        0% {
          transform: translateX(0);
        }

        100% {
<<<<<<< HEAD
          transform: translateX(-50%);
        }
      }

      /* V6 Refined Testimonial Cards */
      .t-card {
        width: 380px;
        white-space: normal;
        position: relative;
        overflow: hidden;
        display: inline-block;
        border-radius: 12px;
        padding: var(--card-pad);
        background: var(--c-base);
        /* Replaced cream-alt with explicit card surface */
        border: 1px solid rgba(15, 23, 42, 0.03);
        border-top: 1px solid rgba(255, 255, 255, 0.8);
        box-shadow: var(--shadow-soft);
        transition: transform var(--dur-hover);
      }

      .t-card:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-float);
      }

      .t-featured {
        width: 440px;
      }

      .t-quote {
        font-size: 1.05rem;
        line-height: 1.6;
        color: var(--c-slate);
        margin-bottom: 2rem;
        position: relative;
        z-index: 2;
      }

      .t-author {
        display: flex;
        align-items: center;
        gap: 1rem;
        z-index: 2;
        position: relative;
      }

      /* ================== V5/V6: 7. EMOTIONAL FINAL CTA ================== */
      .final-cta {
        text-align: center;
        background: var(--c-cream);
        position: relative;
        overflow: hidden;
      }

      /* V6 Warm CTA Glow mapped to Yellow Brand */
      .final-cta::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 60vw;
        height: 60vw;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(255, 184, 0, 0.1) 0%, rgba(0, 168, 255, 0.05) 40%, transparent 70%);
        filter: blur(80px);
        z-index: 0;
        pointer-events: none;
        animation: floatGlow 15s ease-in-out infinite reverse;
      }

      /* ================== V8: PARALLAX BACKGROUND LAYERS ================== */
      .hero-parallax-container {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 0;
        overflow: hidden;
      }

      .parallax-layer {
        position: absolute;
        pointer-events: none;
        transition: transform 0.1s cubic-bezier(0.2, 0.8, 0.2, 1);
        opacity: 0.15;
        /* Much softer background blend */
        z-index: 0;
      }

      .parallax-float {
        width: 100%;
        height: auto;
        display: block;
      }

      /* Push images to the absolute edges to frame the hero content beautifully */
      .p-img-1 {
        top: 10%;
        left: -2%;
        width: 220px;
      }

      .p-img-1 .parallax-float {
        animation: floatGlow 10s infinite alternate;
      }

      .p-img-2 {
        bottom: 5%;
        right: -5%;
        width: 280px;
      }

      .p-img-2 .parallax-float {
        animation: floatGlow 12s infinite alternate-reverse;
      }

      .p-img-3 {
        top: 60%;
        left: -4%;
        width: 140px;
      }

      .p-img-3 .parallax-float {
        animation: floatGlow 8s infinite alternate;
      }

      .p-img-4 {
        top: 5%;
        right: 2%;
        width: 180px;
      }

      .p-img-4 .parallax-float {
        animation: floatGlow 14s infinite alternate-reverse;
      }

      .p-img-5 {
        bottom: 10%;
        left: 35%;
        width: 150px;
        opacity: 0.1;
      }

      .p-img-5 .parallax-float {
        animation: floatGlow 11s infinite alternate;
      }

      .cta-inner {
        max-width: 680px;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        z-index: 2;
        padding: 4rem;
        border-radius: 24px;
      }

      .footer {
        background: var(--c-cream);
        padding: var(--space-section-tb) 0 3rem;
        border-top: 1px solid rgba(15, 23, 42, 0.05);
        position: relative;
      }

      .footer-grid {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr 1.5fr;
        gap: var(--gap-lg);
        margin-bottom: 5rem;
        position: relative;
        z-index: 2;
      }

      .footer-brand .logo {
        font-size: 1.25rem;
        font-weight: 800;
        letter-spacing: -0.02em;
        color: var(--c-navy);
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 1rem;
      }

      .f-col h5 {
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--c-slate-light);
        margin-bottom: 1.5rem;
      }

      .f-col a {
        display: block;
        color: var(--c-slate);
        text-decoration: none;
        margin-bottom: 1rem;
        font-size: 0.95rem;
        font-weight: 400;
        transition: color 0.2s;
      }

      .f-col a:hover {
        color: var(--c-navy);
      }

      .subscribe-box {
        display: flex;
        gap: 0.5rem;
        margin-top: 1rem;
      }

      .subscribe-box input {
        flex: 1;
        padding: 0.6rem 1rem;
        border: 1px solid rgba(15, 23, 42, 0.1);
        border-radius: 6px;
        font-family: inherit;
        font-size: 0.95rem;
        transition: all 0.2s;
        background: var(--c-tint-indigo);
        color: var(--c-navy);
      }

      .subscribe-box input:focus {
        outline: none;
        border-color: var(--c-indigo);
        background: var(--c-base);
        box-shadow: 0 0 0 3px var(--c-indigo-glow);
      }

      .subscribe-box button {
        padding: 0.6rem 1rem;
        background: var(--c-navy);
        color: white;
        border: none;
        border-radius: 6px;
        font-weight: 600;
        cursor: pointer;
        transition: transform 0.1s;
      }

      .subscribe-box button:active {
        transform: scale(0.96);
      }

      .footer-bottom {
        border-top: 1px solid rgba(15, 23, 42, 0.05);
        padding-top: 2rem;
        display: flex;
        justify-content: space-between;
        font-size: 0.85rem;
        color: var(--c-slate-light);
        position: relative;
        z-index: 2;
      }

      @media(max-width: 1024px) {
        .section {
          padding: var(--space-section-tb) 0;
        }

        .hero-grid,
        .pillar-grid,
        .story-block,
        .growth-wrap,
        .com-grid {
          grid-template-columns: 1fr;
          gap: var(--gap-lg);
        }

        .story-block.reversed {
          direction: ltr;
        }

        .hero-content {
          margin: 0 auto;
          text-align: center;
        }

        .hero-actions {
          justify-content: center;
        }

        .hero-pillars {
          justify-content: center;
          flex-wrap: wrap;
        }

        .journey-flow {
          display: none;
        }

        .mockup-base {
          position: relative;
          top: auto;
          left: auto;
          transform: none;
          width: 100%;
          max-width: 500px;
          margin: 0 auto;
          min-height: 320px;
          height: auto;
          animation: none;
        }

        .growth-context {
          padding-right: 0;
          border-right: none;
          border-bottom: 1px solid rgba(0, 0, 0, 0.05);
          padding-bottom: 2rem;
        }

        .footer-grid {
          grid-template-columns: 1fr 1fr;
        }
      }

      @media(max-width: 640px) {
        .section {
          padding: var(--space-section-mb) 0;
        }

        .text-display {
          font-size: 2.75rem;
        }

        .footer-grid {
          grid-template-columns: 1fr;
        }

        .footer-bottom {
          flex-direction: column;
          gap: 1rem;
          text-align: center;
        }
      }

      /* V9 College Carousel */
      .college-carousel {
        position: relative;
        width: 100%;
        height: 180px;
        border-radius: 12px;
        overflow: hidden;
        background: var(--c-slate-light);
        margin-bottom: 20px;
      }

      .carousel-img {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0;
        transition: opacity 0.8s ease-in-out, transform 8s ease-out;
        transform: scale(1.05);
      }

      .carousel-img.active {
        opacity: 1;
        transform: scale(1);
      }

      .carousel-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(to top, rgba(15, 23, 42, 0.4), transparent);
        pointer-events: none;
      }

      /* V7 Override: Prevent carr_style.css from forcing hero h1 to green */
      .hero h1.text-display {
        color: var(--c-navy) !important;
      }

      /* ULTRA PREMIUM SAAS CSS */
      .blueprint-grid {
        position: absolute;
        inset: -20%;
        background-image:
          linear-gradient(rgba(15, 23, 42, 0.03) 1px, transparent 1px),
          linear-gradient(90deg, rgba(15, 23, 42, 0.03) 1px, transparent 1px);
        background-size: 40px 40px;
        z-index: 0;
        pointer-events: none;
      }

      .card-ambient-glow {
        position: absolute;
        inset: 10%;
        background: radial-gradient(circle, rgba(100, 116, 139, 0.15), transparent 70%);
        filter: blur(40px);
        z-index: 0;
        pointer-events: none;
      }

      .premium-glass-card {
        position: relative;
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(12px) saturate(180%);
        -webkit-backdrop-filter: blur(12px) saturate(180%);
        border: 1px solid rgba(255, 255, 255, 0.8);
        border-radius: 18px;
        padding: 24px;
        box-shadow:
          0 20px 40px -10px rgba(15, 23, 42, 0.08),
          0 10px 20px -5px rgba(15, 23, 42, 0.04),
          inset 0 1px 0 rgba(255, 255, 255, 1);
        z-index: 2;
        width: 100%;
        transform-style: preserve-3d;
        animation: slowFloat 6s ease-in-out infinite alternate;
      }

      @keyframes slowFloat {
        0% {
          transform: translateY(0px);
        }

        100% {
          transform: translateY(-12px);
        }
      }

      .edge-light {
        position: absolute;
        inset: 0;
        border-radius: inherit;
        padding: 1px;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.9), transparent 40%, rgba(255, 255, 255, 0.2));
        -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        pointer-events: none;
        transition: background 0.3s;
      }

      .premium-glass-card:hover .edge-light {
        background: linear-gradient(135deg, rgba(255, 255, 255, 1), rgba(167, 139, 250, 0.4) 50%, rgba(255, 255, 255, 0.2));
      }

      .tilt-container {
        transform: perspective(1000px) rotateX(var(--rx, 0deg)) rotateY(var(--ry, 0deg)) translateY(var(--ty, 0px));
        transition: transform 0.1s cubic-bezier(0.2, 0, 0.2, 1), box-shadow 0.3s ease;
      }

      .tilt-container:hover {
        animation: none;
        box-shadow:
          0 30px 60px -15px rgba(15, 23, 42, 0.12),
          0 15px 25px -5px rgba(15, 23, 42, 0.06),
          inset 0 1px 0 rgba(255, 255, 255, 1);
      }

      .parallax-inner {
        transform: translateZ(30px);
        transform-style: preserve-3d;
      }

      .offset-layer {
        transform: translateZ(50px);
      }

      .cinematic-img {
        width: 105%;
        height: 105%;
        object-fit: cover;
        position: absolute;
        top: -2.5%;
        left: -2.5%;
        transition: transform 0.1s ease;
        transform: translate(var(--px, 0px), var(--py, 0px));
      }

      .cinematic-mask {
        position: absolute;
        inset: 0;
        box-shadow: inset 0 0 40px rgba(0, 0, 0, 0.3);
        pointer-events: none;
        border-radius: 12px;
      }

      .probability-wrap {
        background: rgba(15, 23, 42, 0.04);
        padding: 6px 14px;
        border-radius: 20px;
        font-family: 'Inter', sans-serif;
        color: var(--c-navy);
        display: flex;
        align-items: baseline;
        gap: 2px;
        border: 1px solid rgba(15, 23, 42, 0.05);
        transition: box-shadow 0.3s, background 0.3s;
      }

      .probability-wrap.glow-pulse {
        background: rgba(16, 185, 129, 0.1);
        border-color: rgba(16, 185, 129, 0.2);
        box-shadow: 0 0 15px rgba(16, 185, 129, 0.2);
        color: rgba(6, 95, 70, 1);
      }

      .prob-num {
        font-weight: 800;
        font-size: 1.25rem;
        letter-spacing: -0.03em;
      }

      .prob-symbol {
        font-weight: 600;
        font-size: 0.9rem;
      }

      .btn-magnetic {
        position: relative;
        overflow: hidden;
        background: #0F172A;
        color: white;
        box-shadow: 0 4px 14px rgba(15, 23, 42, 0.2);
      }

      .btn-magnetic::before {
        display: none;
      }

      .btn-magnetic .btn-text {
        position: relative;
        z-index: 2;
      }

      .btn-glow-sweep {
        position: absolute;
        top: 0;
        left: -100%;
        width: 50%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transform: skewX(-20deg);
        z-index: 1;
        transition: left 0.5s ease;
      }

      .btn-magnetic:hover .btn-glow-sweep {
        left: 150%;
      }

      .story-pill {
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(15, 23, 42, 0.08);
        background: rgba(255, 255, 255, 0.8);
        box-shadow: 0 2px 8px rgba(15, 23, 42, 0.03);
      }

      .story-pill>* {
        position: relative;
        z-index: 2;
      }

      .pill-shimmer {
        position: absolute;
        top: 0;
        left: -100%;
        width: 50%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.8), transparent);
        animation: pillShimmer 3s infinite;
        z-index: 1;
      }

      @keyframes pillShimmer {
        100% {
          left: 200%;
        }
      }

      .animate-sp-reveal {
        opacity: 0;
        transform: translateY(20px);
        transition: opacity 0.8s cubic-bezier(0.2, 0, 0.2, 1), transform 0.8s cubic-bezier(0.2, 0, 0.2, 1);
      }

      .animate-sp-reveal.is-revealed {
        opacity: 1;
        transform: translateY(0);
      }

      .animate-sp-scale {
        opacity: 0;
        transform: scale(0.96);
        filter: blur(4px);
        transition: opacity 1s cubic-bezier(0.2, 0, 0.2, 1), transform 1s cubic-bezier(0.2, 0, 0.2, 1), filter 1s ease;
      }

      .animate-sp-scale.is-revealed {
        opacity: 1;
        transform: scale(1);
        filter: blur(0);
      }

      #cursor-orb {
        position: fixed;
        top: 0;
        left: 0;
        width: 400px;
        height: 400px;
        margin-left: -200px;
        margin-top: -200px;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(139, 92, 246, 0.12) 0%, rgba(56, 189, 248, 0.08) 40%, transparent 70%);
        pointer-events: none;
        z-index: 9999;
        mix-blend-mode: screen;
        will-change: transform;
        opacity: 0;
        transition: opacity 0.5s ease;
      }
    </style>

    <div class="noise-overlay"></div>

    <svg width="0" height="0" class="absolute pointer-events-none" style="position: absolute; z-index: -1;">
      <filter id="lightning-filter" x="-20%" y="-20%" width="140%" height="140%">
        <feTurbulence type="fractalNoise" baseFrequency="0.03" numOctaves="3" result="noise">
          <animate attributeName="baseFrequency" values="0.03; 0.05; 0.03" dur="0.15s" repeatCount="indefinite" />
        </feTurbulence>
        <feDisplacementMap in="SourceGraphic" in2="noise" scale="6" xChannelSelector="R" yChannelSelector="G" />
      </filter>
    </svg>

    <main>
      <!-- V5/V8 SECTION 1: HERO & PLATFORM OVERVIEW (With Restored Parallax) -->
      <section class="hero section" id="hero-section">

        <!-- V8 Restored Parallax Layers -->
        <div class="hero-parallax-container">
          <div class="parallax-layer p-img-1" data-speed="0.08"><img src="/img/hero_parallax/hero-1.png"
              class="parallax-float" alt=""></div>
          <div class="parallax-layer p-img-2" data-speed="-0.05"><img src="/img/hero_parallax/hero-2.png"
              class="parallax-float" alt=""></div>
          <div class="parallax-layer p-img-3" data-speed="0.10"><img src="/img/hero_parallax/hero-3.png"
              class="parallax-float" alt=""></div>
          <div class="parallax-layer p-img-4" data-speed="-0.07"><img src="/img/hero_parallax/hero-4.png"
              class="parallax-float" alt=""></div>
          <div class="parallax-layer p-img-5" data-speed="0.06"><img src="/img/hero_parallax/hero-5.png"
              class="parallax-float" alt=""></div>
        </div>

        <div class="container hero-grid">
          <div class="hero-content">
            <span class="text-eyebrow">EduNavigator Platform</span>
            <h1 class="text-display mb-medium">Precision career architecture.</h1>
            <p class="text-lead mb-medium">An all-in-one platform built for ambitious students. We combine predictive
              models, technical assessments, and expert resources to map your highest-leverage career trajectory.</p>
            <div class="hero-actions">
              <a href="/register" class="btn btn-primary">Start Assessment</a>
              <a href="/integrations" class="btn btn-outline">Explore Tools</a>
            </div>

            <!-- Pillar Overview Indicators -->
            <div class="hero-pillars">
              <div class="hp-item"><i class="fas fa-layer-group"></i> Features</div>
              <div class="hp-item"><i class="fas fa-wrench"></i> Tools</div>
              <div class="hp-item"><i class="fas fa-chart-line"></i> Assessment</div>
              <div class="hp-item"><i class="fas fa-book-open"></i> Resources</div>
            </div>
          </div>

          <div class="hero-visual">
            <div class="mockup-base glass-acrylic">
              <div class="mock-nav">
                <div class="mock-dots"><span></span><span></span><span></span></div>
                <div class="text-small" style="font-weight:600; color:var(--c-navy);">Workspace Overview</div>
                <div style="width:24px; height:24px; border-radius:50%; background:rgba(0,0,0,0.08);"></div>
              </div>
              <div class="mock-body">
                <div class="mock-skeleton" style="width: 120px;"></div>
                <div class="mock-skeleton" style="width: 200px;"></div>

                <div class="mock-internal-card">
                  <div
                    style="width:40px; height:40px; border-radius:8px; background:var(--c-indigo-soft); color:var(--c-indigo); display:flex; align-items:center; justify-content:center;">
                    <i class="fas fa-brain"></i>
                  </div>
                  <div style="flex:1;">
                    <div style="font-size:0.8rem; font-weight:600; color:var(--c-navy); margin-bottom:4px;">Cognitive
                      Readiness</div>
                    <div class="mock-skeleton" style="width:80%; margin:0; height:6px;"></div>
                  </div>
                  <div style="font-size:0.85rem; font-weight:700; color:var(--c-teal);">Strong</div>
                </div>

                <div class="mock-internal-card">
                  <div
                    style="width:40px; height:40px; border-radius:8px; background:var(--c-coral-soft); color:var(--c-coral); display:flex; align-items:center; justify-content:center;">
                    <i class="fas fa-briefcase"></i>
                  </div>
                  <div style="flex:1;">
                    <div style="font-size:0.8rem; font-weight:600; color:var(--c-navy); margin-bottom:4px;">Market Match
                    </div>
                    <div class="mock-skeleton" style="width:60%; margin:0; height:6px;"></div>
                  </div>
                  <div style="font-size:0.85rem; font-weight:700; color:var(--c-amber);">Evaluating</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- V5/V10 SECTION 2: CORE PLATFORM PILLARS -->
      <section class="hub-section section edge-top" style="position: relative;">
        <!-- Animated Pink Waves Layer (User Provided) -->
        <div class="absolute bottom-[-10px] left-0 w-full h-[600px] z-0 overflow-hidden pointer-events-none"
          style="mix-blend-mode: multiply;">
          <svg style="width:100%; height:100%;" viewBox="0 0 2 1" preserveAspectRatio="none">
            <defs>
              <path id="custom-wave" d="m0 1v-.5 q.5.5 1 0 t1 0 1 0 1 0 v.5z" />
            </defs>
            <g>
              <use href="#custom-wave" y=".0" fill="rgba(251, 207, 232, 0.4)" class="waves-use-el" />
              <use href="#custom-wave" y=".1" fill="rgba(249, 168, 212, 0.5)" class="waves-use-el" />
              <use href="#custom-wave" y=".2" fill="rgba(244, 114, 182, 0.6)" class="waves-use-el" />
            </g>
          </svg>
        </div>
        <!-- V10 Animated Network Connectors -->
        <div class="hub-connectors">
          <svg viewBox="0 0 1200 800" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
            <defs>
              <linearGradient id="network-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="var(--c-indigo)" stop-opacity="0.5" />
                <stop offset="50%" stop-color="var(--c-teal)" stop-opacity="0.5" />
                <stop offset="100%" stop-color="var(--c-amber)" stop-opacity="0.5" />
              </linearGradient>
            </defs>
            <path class="animated-path" d="M 100 100 C 400 300, 800 100, 1100 300" />
            <path class="animated-path" d="M 100 700 C 400 500, 800 700, 1100 500" />
            <path class="animated-path" d="M 100 100 C 300 400, 300 800, 100 700" />
            <path class="animated-path" d="M 1100 100 C 900 400, 900 800, 1100 700" />
            <path class="animated-path" d="M 300 200 C 600 400, 600 600, 900 200" />
            <path class="animated-path" d="M 300 600 C 600 400, 600 200, 900 600" />
          </svg>
        </div>
        <div class="container relative">
          <div class="text-center mb-large" style="max-width:600px; margin:0 auto 4rem auto;">
            <span class="text-eyebrow animate-reveal-up-1">The Hub</span>
            <h2 class="text-h2 mb-medium animate-reveal-up-2 animate-text-crystal">Everything connects.</h2>
            <p class="text-body animate-reveal-up-3" style="margin:0 auto; max-width: 52ch;">Not just independent tools.
              The platform is organized into four
              interconnected pillars designed to guide you from discovery to placement.</p>
          </div>

          <div class="pillar-grid">
            <!-- Features Pillar -->
            <div class="pillar-card electric-card-wrapper pc-features">
              <div class="pc-header">
                <div class="pc-icon-wrap"><i class="fas fa-compass"></i></div>
                <div>
                  <h3 class="text-h3">Discovery Features</h3>
                  <p class="text-body" style="font-size:0.95rem; line-height:1.4;">Find direction based on hard market
                    data and predictive algorithms.</p>
                </div>
              </div>
              <div class="pc-list">
                <div class="pc-list-item"><i class="fas fa-check"></i> College Predictor Engine</div>
                <div class="pc-list-item"><i class="fas fa-check"></i> AI Career Trends Analysis</div>
                <div class="pc-list-item"><i class="fas fa-check"></i> Skills & Interests Mapping</div>
                <div class="pc-list-item"><i class="fas fa-check"></i> Curated Career Bank</div>
              </div>
            </div>

            <!-- Tools Pillar -->
            <div class="pillar-card electric-card-wrapper pc-tools">
              <div class="pc-header">
                <div class="pc-icon-wrap"><i class="fas fa-cubes"></i></div>
                <div>
                  <h3 class="text-h3">Execution Tools</h3>
                  <p class="text-body" style="font-size:0.95rem; line-height:1.4;">Build your profile, practice live
                    interviews, and generate targeted resumes.</p>
                </div>
              </div>
              <div class="pc-list">
                <div class="pc-list-item"><i class="fas fa-arrow-right"></i> Dynamic Resume Builder</div>
                <div class="pc-list-item"><i class="fas fa-arrow-right"></i> Live AI Interview Simulator</div>
                <div class="pc-list-item"><i class="fas fa-arrow-right"></i> GitHub Profile Analyzer</div>
                <div class="pc-list-item"><i class="fas fa-arrow-right"></i> 24/7 AI Career Chat</div>
              </div>
            </div>

            <!-- Assessment Pillar -->
            <div class="pillar-card electric-card-wrapper pc-assess">
              <div class="pc-header">
                <div class="pc-icon-wrap"><i class="fas fa-chart-pie"></i></div>
                <div>
                  <h3 class="text-h3">Assessment Lab</h3>
                  <p class="text-body" style="font-size:0.95rem; line-height:1.4;">Benchmark your quantitative and
                    verbal skills against global cohorts.</p>
                </div>
              </div>
              <div class="pc-list">
                <div class="pc-list-item"><i class="fas fa-circle-notch"></i> Adaptive Smart Quiz Format</div>
                <div class="pc-list-item"><i class="fas fa-circle-notch"></i> Core Aptitude Scoring</div>
                <div class="pc-list-item"><i class="fas fa-circle-notch"></i> Real-time Leaderboard</div>
              </div>
            </div>

            <!-- Resources Pillar -->
            <div class="pillar-card electric-card-wrapper pc-resources">
              <div class="pc-header">
                <div class="pc-icon-wrap"><i class="fas fa-book"></i></div>
                <div>
                  <h3 class="text-h3">Resource Library</h3>
                  <p class="text-body" style="font-size:0.95rem; line-height:1.4;">Access deep-dive roadmaps, secure
                    funding, and find mentorship.</p>
                </div>
              </div>
              <div class="pc-list">
                <div class="pc-list-item"><i class="fas fa-bookmark"></i> Specialized Career Roadmaps</div>
                <div class="pc-list-item"><i class="fas fa-bookmark"></i> Scholarship Database</div>
                <div class="pc-list-item"><i class="fas fa-bookmark"></i> Live Industry Webinars</div>
                <div class="pc-list-item"><i class="fas fa-bookmark"></i> Mentorship Connection</div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- V5 SECTION 3: FEATURE SPOTLIGHT STORYTELLING -->
      <section class="story-section section edge-top">
        <div class="container">

          <!-- Spotlight 1: College Predictor (Ultra Premium) -->
          <div class="story-block ultra-premium-sp">
            <div class="story-text">
              <div class="story-pill animate-sp-reveal" style="animation-delay: 0.1s;">
                <div class="pill-shimmer"></div>
                <i class="fas fa-university" style="color:var(--c-indigo);"></i> Discovery Engine
              </div>
              <h2 class="text-h2 mb-medium animate-sp-reveal" style="animation-delay: 0.2s; letter-spacing: -0.02em;">
                Algorithmically map your acceptance chances.</h2>
              <p class="text-body mb-medium animate-sp-reveal" style="animation-delay: 0.35s; max-width: 560px;">Don't
                rely on guesswork. Our college predictor uses historical admission
                data for JEE and MHT-CET to instantly calculate your probability of acceptance across top institutions
                based on your specific percentile and category.</p>
              <div class="animate-sp-reveal" style="animation-delay: 0.5s;">
                <a href="/exam_choice" class="btn btn-primary btn-magnetic">
                  <span class="btn-text">Try Predictor</span>
                  <div class="btn-glow-sweep"></div>
                </a>
              </div>
            </div>

            <div class="story-visual-wrap animate-sp-scale"
              style="animation-delay: 0.2s; background: transparent; border: none; box-shadow: none;">
              <!-- Subtle blueprint background grid -->
              <div class="blueprint-grid"></div>

              <!-- Soft radial glow behind card -->
              <div class="card-ambient-glow"></div>

              <!-- Floating Prediction Card (3D Tilt Container) -->
              <div class="premium-glass-card tilt-container" id="predictor-card">
                <!-- Edge Light effect -->
                <div class="edge-light"></div>

                <div class="college-carousel parallax-inner cinematic-image-wrap"
                  style="border-radius:12px; overflow:hidden; box-shadow: 0 4px 20px rgba(0,0,0,0.05); aspect-ratio: 16/9; position:relative;">
                  <img src="/img/colleges/media__1771916962641.jpg" class="carousel-img active cinematic-img"
                    alt="COEP">
                  <img src="/img/colleges/media__1771916962661.jpg" class="carousel-img cinematic-img" alt="VJTI">
                  <img src="/img/colleges/media__1771916962727.jpg" class="carousel-img cinematic-img" alt="PICT">
                  <img src="/img/colleges/media__1771916962745.jpg" class="carousel-img cinematic-img" alt="SPIT">
                  <div class="cinematic-mask"></div>
                </div>

                <div
                  style="display:flex; justify-content:space-between; align-items:center; border-top:1px solid rgba(15, 23, 42, 0.06); padding-top:18px; margin-top:20px;"
                  class="parallax-inner offset-layer">
                  <div id="predict-name"
                    style="font-weight:600; color:var(--c-navy); font-size: 1.05rem; font-family: 'Inter', sans-serif;">
                    COEP Tech (CS)</div>
                  <div class="probability-wrap" id="predict-prob-wrap">
                    <span id="predict-prob-num" class="prob-num">0</span><span class="prob-symbol">% Prob.</span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Spotlight 2: Profile Analyzer (Reversed) -->
          <div class="story-block reversed">
            <div class="story-text">
              <div class="story-pill"><i class="fab fa-github" style="color:var(--c-navy);"></i> Technical Parsing</div>
              <h2 class="text-h2 mb-medium">Evaluate code quality, not just bullet points.</h2>
              <p class="text-body mb-medium">Hook up your GitHub. The Profile Analyzer breaks down your repositories,
                determining complexity, architectural patterns, and commit consistency to prove you actually know how to
                build.</p>
              <a href="/integrations" class="btn btn-outline">Analyze Profile</a>
            </div>
            <div class="story-visual-wrap" style="background:var(--c-navy);">
              <div style="width:80%; padding:20px; font-family:monospace; color:#A0AEC0; font-size:0.85rem;">
                <div>{</div>
                <div style="padding-left:20px;">"user": "<span style="color:#60A5FA;">developer_01</span>",</div>
                <div style="padding-left:20px;">"architectural_score": <span style="color:#A78BFA;">92</span>,</div>
                <div style="padding-left:20px;">"languages": ["<span style="color:#34D399;">TS</span>", "<span
                    style="color:#34D399;">Rust</span>"],</div>
                <div style="padding-left:20px;">"commit_volume": <span style="color:#A78BFA;">"High"</span></div>
                <div>}</div>
              </div>
            </div>
          </div>

          <!-- Spotlight 3: AI Interview Simulator -->
          <div class="story-block">
            <div class="story-text">
              <div class="story-pill"><i class="fas fa-microphone" style="color:var(--c-coral);"></i> Live Execution
              </div>
              <h2 class="text-h2 mb-medium">Live stress-testing before the real thing.</h2>
              <p class="text-body mb-medium">The AI Interview Simulator listens, responds dynamically, and grades your
                answers. Choose from System Design, DSA, or Behavioral rounds and get instant tactical feedback on your
                delivery.</p>
              <a href="/interview-simulator" class="btn btn-outline">Start Simulator</a>
            </div>
            <div class="story-visual-wrap" style="padding: 0; overflow: hidden; border-radius: 20px;">
              <div class="sv-bg-grid"></div>
              <img src="/img/features/interview_sim.jpg" alt="AI Interview Simulator"
                style="width: 100%; height: 100%; object-fit: cover; border-radius: 20px; transition: transform 0.5s ease;"
                class="hover-scale-img">
            </div>
          </div>

        </div>
      </section>

      <!-- V5 SECTION 4: ECOSYSTEM HORIZONTAL JOURNEY -->
      <section class="journey-section section edge-top">
        <div class="container relative">
          <div class="text-center mb-large">
            <span class="text-eyebrow">The Pipeline</span>
            <h2 class="text-h2">How it all fits together.</h2>
          </div>

          <div class="journey-flow">
            <div class="j-line"></div>
            <div class="j-node">
              <div class="j-dot">1</div>
              <div class="j-title">Discover</div>
              <div class="j-desc">Career Trends & Assess</div>
            </div>
            <div class="j-node">
              <div class="j-dot">2</div>
              <div class="j-title">Analyze</div>
              <div class="j-desc">Identify cognitive gaps</div>
            </div>
            <div class="j-node">
              <div class="j-dot">3</div>
              <div class="j-title">Improve</div>
              <div class="j-desc">Interviews & Roadmaps</div>
            </div>
            <div class="j-node">
              <div class="j-dot">4</div>
              <div class="j-title">Build</div>
              <div class="j-desc">Algorithmic Resumes</div>
            </div>
            <div class="j-node">
              <div class="j-dot">5</div>
              <div class="j-title">Apply</div>
              <div class="j-desc">College Predictor insights</div>
            </div>
          </div>
        </div>
      </section>

      <!-- V5 SECTION 5: PERSONAL GROWTH HUB (Analytics Integration) -->
      <section class="growth-section section edge-top">
        <div class="container growth-wrap">

          <div class="growth-context">
            <span class="text-eyebrow">The Dashboard</span>
            <h2 class="text-h2 mb-medium">Metrics that dictate your next move.</h2>
            <p class="text-lead mb-medium">What happens after you use the tools? The platform aggregates your data into
              a unified dashboard.</p>
            <p class="text-body mb-medium">We don't just score you—we synthesize your assessment logic, your interview
              performance, and your GitHub commits to identify exactly what you need to study next to remain globally
              competitive.</p>
            <div style="display:flex; gap:16px; margin-top:2rem;">
              <div style="display:flex; gap:8px; align-items:center; font-size:0.9rem; font-weight:600;"><i
                  class="fas fa-shield-alt text-slate"></i> Verified Data</div>
              <div style="display:flex; gap:8px; align-items:center; font-size:0.9rem; font-weight:600;"><i
                  class="fas fa-lock text-slate"></i> Private Context</div>
            </div>
          </div>

          <!-- Glass Analytics Board Preserved from V4 -->
          <div class="analytics-board">
            <div class="a-sheet glass-acrylic"></div>
            <div class="a-stat-pill"><i class="fas fa-bolt"></i> Analysis Ready</div>

            <div class="a-card-main">
              <div class="text-eyebrow" style="margin-bottom:0;">Core Synthesis</div>
              <div class="a-ring-wrap">
                <svg class="a-ring-svg" viewBox="0 0 160 160">
                  <circle class="a-ring-bg" cx="80" cy="80" r="70"></circle>
                  <circle class="a-ring-fg" cx="80" cy="80" r="70" id="dash-ring"></circle>
                </svg>
                <div class="a-score" id="dash-score">0</div>
              </div>
              <div
                style="display:flex; gap:2rem; padding-top:1.5rem; border-top:1px solid rgba(0,0,0,0.04); width:100%; justify-content:center;">
                <div style="text-align:center;">
                  <div style="font-size:1.25rem; font-weight:700;">89%</div>
                  <div class="text-small">Verbal</div>
                </div>
                <div style="text-align:center;">
                  <div style="font-size:1.25rem; font-weight:700;">94%</div>
                  <div class="text-small">Logic</div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </section>

      <!-- V5 SECTION 6: COMMUNITY & PROOF -->
      <section class="community-section section edge-top">
        <div class="container">

          <div class="com-grid">
            <!-- Text & Stats -->
            <div>
              <span class="text-eyebrow">Community</span>
              <h2 class="text-h2 mb-medium">You are not building in isolation.</h2>
              <p class="text-body mb-large">Join thousands of students competing on the global leaderboard and
                redefining their trajectories through peer momentum.</p>

              <div style="display:grid; grid-template-columns:1fr 1fr; gap:24px;">
                <div>
                  <div style="font-size:2.5rem; font-weight:800; color:var(--c-navy); line-height:1;">12k+</div>
                  <div class="text-small">Resumes Generated</div>
                </div>
                <div>
                  <div style="font-size:2.5rem; font-weight:800; color:var(--c-navy); line-height:1;">Top 5%</div>
                  <div class="text-small">Placement Rate</div>
                </div>
              </div>
            </div>

            <!-- Leaderboard Glass Visual -->
            <div class="glass-acrylic lb-preview">
              <div class="text-eyebrow" style="margin-left:8px; margin-bottom:12px;">Global Leaderboard (Live)</div>
              <div class="lb-row" style="background:#FFF;">
                <div class="lb-user">
                  <div class="lb-avatar"
                    style="background:var(--c-indigo-soft); color:var(--c-indigo); display:flex; align-items:center; justify-content:center; font-size:0.75rem;">
                    1</div> <span style="font-weight:600;">Arjun K.</span>
                </div>
                <div class="lb-score">98.4</div>
              </div>
              <div class="lb-row" style="opacity:0.9;">
                <div class="lb-user">
                  <div class="lb-avatar"></div> <span>Priya M.</span>
                </div>
                <div class="lb-score" style="color:var(--c-slate);">96.2</div>
              </div>
              <div class="lb-row" style="opacity:0.7;">
                <div class="lb-user">
                  <div class="lb-avatar"></div> <span>Sam T.</span>
                </div>
                <div class="lb-score" style="color:var(--c-slate);">94.1</div>
              </div>
            </div>
          </div>

          <!-- Preserved V4 Testimonials Marquee -->
          <div class="test-marquee">
            <div class="test-track">
              <div class="t-card" style="box-shadow:var(--shadow-micro);">
                <div class="t-watermark">"</div>
                <p class="t-quote">The aptitude synthesis spotted a lateral pivot into DevRel that perfectly aligned
                  with my quantitative baseline.</p>
                <div class="t-author">
                  <div class="t-avatar"></div>
                  <div>
                    <div style="font-weight:600; font-size:0.9rem;">Elena R.</div>
                    <div class="text-small">DevRel, Vercel</div>
                  </div>
                </div>
              </div>

              <!-- Glass Testimonial -->
              <div class="glass-acrylic glass-lift t-card t-featured">
                <div class="t-watermark">"</div>
                <p class="t-quote" style="color:var(--c-navy); font-weight:500;">Ran through the AI Simulator here for
                  three weeks, completely rebuilt my narrative structure, and passed easily.</p>
                <div class="t-author">
                  <div class="t-avatar" style="background:#F1F5F9;"></div>
                  <div>
                    <div style="font-weight:600; font-size:0.9rem;">Marcus J.</div>
                    <div class="text-small">Senior SWE, Meta</div>
                  </div>
                </div>
              </div>

              <div class="t-card" style="box-shadow:var(--shadow-micro);">
                <div class="t-watermark">"</div>
                <p class="t-quote">Connecting my GitHub to the Profile Analyzer instantly bypassed the generic resume
                  screen. The metrics spoke for themselves.</p>
                <div class="t-author">
                  <div class="t-avatar"></div>
                  <div>
                    <div style="font-weight:600; font-size:0.9rem;">David L.</div>
                    <div class="text-small">Backend Dev</div>
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </section>

      <!-- V5 SECTION 7: EMOTIONAL CTA -->
      <section class="final-cta section b-white edge-top">
        <div class="container relative">
          <div class="glass-acrylic cta-inner">
            <h2 class="text-h1 mb-medium" style="position:relative; z-index:2;">Ready to compile your career?</h2>
            <p class="text-lead mb-large" style="position:relative; z-index:2;">Stop guessing. Start measuring. Join the
              engineers building verified trajectories.</p>
            <div style="display:flex; gap:var(--gap-sm); position:relative; z-index:2;">
              <a href="/register" class="btn btn-primary">Start Your Journey</a>
              <a href="/login" class="btn btn-outline" style="background:white;">Sign In</a>
            </div>
          </div>
        </div>
      </section>
    </main>

    <%- include('layouts/footer'); %>
      <!-- Note: The footer layout logic was pulled into layouts/footer if it exists, otherwise I'll restore the hardcoded footer if prefered, but for cleanliness assuming includes/footer applies. I will append the hardcoded V4 footer below to guarantee nothing breaks. -->
      <footer class="footer">
        <div class="container">
          <div class="footer-grid">
            <div class="footer-brand" style="grid-column: span 2;">
              <div class="logo">
                <div style="width:20px; height:20px; background:var(--c-navy); border-radius:4px;"></div>
                EduNavigator
              </div>
            </div>
            <div class="f-col">
              <h5>Platform</h5>
              <a href="/integrations">Profile Analyzer</a>
              <a href="/interview-simulator">AI Interviewer</a>
              <a href="/resume-builder">Algorithmic Resume</a>
            </div>
            <div class="f-col">
              <h5>Company</h5>
              <a href="#">Manifesto</a>
              <a href="#">Security</a>
              <a href="#">Contact</a>
            </div>
            <div class="f-col" style="grid-column: span 1.5;">
              <h5>Updates</h5>
              <form class="subscribe-box" onsubmit="event.preventDefault();">
                <input type="email" placeholder="example@domain.com" required>
                <button type="submit">Subscribe</button>
              </form>
            </div>
          </div>
          <div class="footer-bottom">
            <div>&copy; 2026 EduNavigator. All schemas reserved.</div>
            <div style="display:flex; gap:var(--gap-md);">
              <a href="#"><i class="fab fa-twitter"></i></a>
              <a href="#"><i class="fab fa-github"></i></a>
              <a href="#"><i class="fab fa-linkedin"></i></a>
            </div>
          </div>
        </div>
      </footer>

      <script>
        // Shared Intersection logic
        document.addEventListener('DOMContentLoaded', () => {
          // Analytics Ring
          const ring = document.getElementById('dash-ring');
          const scoreVal = document.getElementById('dash-score');
          if (ring && scoreVal) {
            const targetScore = 93;
            const observer = new IntersectionObserver((entries) => {
              if (entries[0].isIntersecting) {
                setTimeout(() => {
                  ring.style.strokeDashoffset = '30';
                  let current = 0;
                  const interval = setInterval(() => {
                    current += 1;
                    if (current >= targetScore) { current = targetScore; clearInterval(interval); }
                    scoreVal.innerText = current;
                  }, 9);
                }, 100);
                observer.disconnect();
              }
            }, { threshold: 0.6 });
            observer.observe(document.querySelector('.growth-wrap'));
          }

          // V8: Hero Parallax Javascript Engine
          const heroSection = document.getElementById('hero-section');
          const parallaxLayers = document.querySelectorAll('.parallax-layer');

          if (heroSection && parallaxLayers.length > 0) {
            heroSection.addEventListener('mousemove', (e) => {
              // Calculate center of window representing the hero
              const rect = heroSection.getBoundingClientRect();
              const centerX = rect.left + rect.width / 2;
              const centerY = rect.top + rect.height / 2;

              const mouseX = e.clientX - centerX;
              const mouseY = e.clientY - centerY;

              // Smoothly map mouse to translation based on data-speed
              parallaxLayers.forEach(layer => {
                const speed = parseFloat(layer.getAttribute('data-speed')) || 0;
                // Stronger effect limits to prevent flying completely off-screen
                const xPos = Math.max(-50, Math.min(50, -(mouseX * speed)));
                const yPos = Math.max(-50, Math.min(50, -(mouseY * speed)));

                // Directly set the inline transform
                layer.style.transform = `translate3d(${xPos}px, ${yPos}px, 0)`;
              });
            });

            heroSection.addEventListener('mouseleave', () => {
              parallaxLayers.forEach(layer => {
                layer.style.transform = `translate3d(0, 0, 0)`;
              });
            });
          }

          // V9: College Predictor Carousel Engine
          const collegeData = [
            { name: "COEP Tech (CS)", prob: "98.4" },
            { name: "VJTI Mumbai (IT)", prob: "95.2" },
            { name: "PICT Pune (CE)", prob: "92.1" },
            { name: "SPIT Mumbai (CS)", prob: "89.7" }
          ];
          const slides = document.querySelectorAll('.carousel-img');
          const nameEl = document.getElementById('predict-name');
          const probEl = document.getElementById('predict-prob-num');

          if (slides.length > 0 && nameEl && probEl) {
            let currentSlide = 0;
            setInterval(() => {
              nameEl.style.opacity = 0;
              probEl.parentElement.style.opacity = 0;

              setTimeout(() => {
                slides[currentSlide].classList.remove('active');
                currentSlide = (currentSlide + 1) % slides.length;
                slides[currentSlide].classList.add('active');

                nameEl.innerText = collegeData[currentSlide].name;
                probEl.innerText = collegeData[currentSlide].prob;

                nameEl.style.opacity = 1;
                probEl.parentElement.style.opacity = 1;
              }, 300);
            }, 3500);
          }

          // ULTRA SASS PREMIUM JS 
          // 1. Cursor Orb
          const orb = document.getElementById('cursor-orb');
          let mouseX = window.innerWidth / 2;
          let mouseY = window.innerHeight / 2;
          let orbX = mouseX;
          let orbY = mouseY;

          window.addEventListener('mousemove', (e) => {
            mouseX = e.clientX;
            mouseY = e.clientY;
            if (orb && orb.style.opacity == 0) orb.style.opacity = 1;
          });

          function renderOrb() {
            orbX += (mouseX - orbX) * 0.1;
            orbY += (mouseY - orbY) * 0.1;
            if (orb) orb.style.transform = `translate3d(${orbX}px, ${orbY}px, 0)`;
            requestAnimationFrame(renderOrb);
          }
          if (orb) renderOrb();

          // 2. 3D Tilt Card Component
          const tiltCard = document.getElementById('predictor-card');
          if (tiltCard) {
            tiltCard.addEventListener('mousemove', (e) => {
              const rect = tiltCard.getBoundingClientRect();
              const x = e.clientX - rect.left;
              const y = e.clientY - rect.top;

              const centerX = rect.width / 2;
              const centerY = rect.height / 2;

              const rotateX = ((y - centerY) / centerY) * -6;
              const rotateY = ((x - centerX) / centerX) * 6;

              tiltCard.style.setProperty('--rx', `${rotateX}deg`);
              tiltCard.style.setProperty('--ry', `${rotateY}deg`);

              const px = ((x - centerX) / centerX) * -8;
              const py = ((y - centerY) / centerY) * -8;
              tiltCard.querySelectorAll('.cinematic-img').forEach(img => {
                img.style.setProperty('--px', `${px}px`);
                img.style.setProperty('--py', `${py}px`);
              });
            });

            tiltCard.addEventListener('mouseleave', () => {
              tiltCard.style.setProperty('--rx', `0deg`);
              tiltCard.style.setProperty('--ry', `0deg`);
              tiltCard.querySelectorAll('.cinematic-img').forEach(img => {
                img.style.setProperty('--px', `0px`);
                img.style.setProperty('--py', `0px`);
              });
            });
          }

          // 3. Number Counter & Scroll Reveal
          const spRevealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-revealed');

                if (entry.target.classList.contains('story-visual-wrap')) {
                  const probEl2 = document.getElementById('predict-prob-num');
                  const wrap = document.getElementById('predict-prob-wrap');
                  if (probEl2 && wrap && probEl2.innerText == '0') {
                    setTimeout(() => {
                      let current = 0;
                      const target = 98.4;
                      const duration = 1200;
                      const startTime = performance.now();

                      function animateCounter(currentTime) {
                        const elapsed = currentTime - startTime;
                        const progress = Math.min(elapsed / duration, 1);
                        const easeOut = progress === 1 ? 1 : 1 - Math.pow(2, -10 * progress);
                        current = target * easeOut;
                        probEl2.innerText = current.toFixed(1);

                        if (progress < 1) {
                          requestAnimationFrame(animateCounter);
                        } else {
                          wrap.classList.add('glow-pulse');
                        }
                      }
                      requestAnimationFrame(animateCounter);
                    }, 600);
                  }
                }

                spRevealObserver.unobserve(entry.target);
              }
            });
          }, { threshold: 0.15 });

          document.querySelectorAll('.animate-sp-reveal, .animate-sp-scale').forEach(el => {
            spRevealObserver.observe(el);
          });

          // 4. Magnetic Buttons
          document.querySelectorAll('.btn-magnetic').forEach(btn => {
            btn.addEventListener('mousemove', (e) => {
              const rect = btn.getBoundingClientRect();
              const x = e.clientX - rect.left - rect.width / 2;
              const y = e.clientY - rect.top - rect.height / 2;
              btn.style.transform = `translate(${x * 0.2}px, ${y * 0.2}px)`;
            });
            btn.addEventListener('mouseleave', () => {
              btn.style.transform = `translate(0px, 0px)`;
              btn.style.transition = 'transform 0.5s cubic-bezier(0.2, 0, 0.2, 1)';
            });
            btn.addEventListener('mouseenter', () => {
              btn.style.transition = 'none'; // remove transition for smooth tracking
            });
          });

          // V10: High-End Mouse Tracking Spotlight & 3D Tilt for Hub Cards
          const hubCards = document.querySelectorAll('.pillar-card');
          if (hubCards.length > 0) {
            hubCards.forEach(card => {
              card.addEventListener('mousemove', e => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                card.style.setProperty('--mouse-x', `${x}px`);
                card.style.setProperty('--mouse-y', `${y}px`);
                const centerX = rect.width / 2;
                const centerY = rect.height / 2;
                const rotateX = ((y - centerY) / centerY) * -5;
                const rotateY = ((x - centerX) / centerX) * 5;
                card.style.setProperty('--rotate-x', `${rotateX}deg`);
                card.style.setProperty('--rotate-y', `${rotateY}deg`);
              });
              card.addEventListener('mouseleave', () => {
                card.style.setProperty('--rotate-x', `0deg`);
                card.style.setProperty('--rotate-y', `0deg`);
              });
            });

            // IntersectionObserver setup
            const hubObserver = new IntersectionObserver((entries) => {
              entries.forEach(entry => {
                if (entry.isIntersecting) {
                  entry.target.classList.add('revealed');
                  hubObserver.unobserve(entry.target);
                  // Remove transition delay after animation completes to avoid hover lag
                  setTimeout(() => {
                    entry.target.style.transitionDelay = '0s';
                  }, 800);
                }
              });
            }, { threshold: 0.15 });

            hubCards.forEach((card, index) => {
              card.style.transitionDelay = `${index * 0.15}s`;
              hubObserver.observe(card);
            });
          }

          // Scroll Bounce Observer for Hub Text
          const bounceTextObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-revealed');
                bounceTextObserver.unobserve(entry.target);
              }
            });
          }, { threshold: 0.1 });

          document.querySelectorAll('.animate-reveal-up-1, .animate-reveal-up-2, .animate-reveal-up-3').forEach(el => {
            bounceTextObserver.observe(el);
          });

        });
      </script>
      <div id="cursor-orb"></div>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
      </body>
=======
          transform: translateX(calc(-350px * 5 - 2rem * 5));
        }
      }

      .premium-card {
        border: 1px solid rgba(0, 0, 0, 0.05);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
        transition: all 0.4s cubic-bezier(0.25, 1, 0.5, 1);
        display: flex;
        flex-direction: column;
        height: 100%;
      }

      @keyframes gradientShift {
        0% {
          background-position: 0% 50%;
        }

        50% {
          background-position: 100% 50%;
        }

        100% {
          background-position: 0% 50%;
        }
      }

      .premium-gradient-bg {
        background: linear-gradient(-45deg, #FFB800, #FF9F00, #00A8FF, #6C63FF);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
      }
    </style>
>>>>>>> origin/Teachers-students-connect

    <%- include('layouts/footer'); %>