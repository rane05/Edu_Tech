<%- include('layouts/header'); %>
<%- include('layouts/navbar'); %>

<!-- Dynamic Loading Animation -->
<style>
  .loading-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #0f172a 0%, #1e1b4b 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
    transition: opacity 0.5s ease, visibility 0.5s ease;
  }

  .loading-overlay.hidden {
    opacity: 0;
    visibility: hidden;
  }

  .loading-content {
    text-align: center;
    color: white;
  }

  .loading-logo {
    width: 80px;
    height: 80px;
    margin: 0 auto 2rem;
    position: relative;
    animation: logoFloat 2s ease-in-out infinite;
  }

  .loading-logo::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border-radius: 20px;
    animation: logoRotate 3s linear infinite;
  }

  .loading-logo::after {
    content: 'E';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 2rem;
    font-weight: bold;
    color: white;
    z-index: 1;
  }

  @keyframes logoFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @keyframes logoRotate {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }

  .loading-text {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .loading-bar {
    width: 200px;
    height: 4px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 2px;
    margin: 0 auto;
    overflow: hidden;
    position: relative;
  }

  .loading-progress {
    height: 100%;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    border-radius: 2px;
    width: 0%;
    transition: width 0.3s ease;
    position: relative;
  }

  .loading-progress::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
    animation: shimmer 1.5s ease-in-out infinite;
  }

  @keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
  }

  /* Page entrance animations */
  .page-entrance {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .page-entrance.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Enhanced cursor effects */
  .enhanced-cursor {
    position: fixed;
    width: 30px;
    height: 30px;
    background: radial-gradient(circle, rgba(79, 70, 229, 0.8) 0%, rgba(236, 72, 153, 0.4) 100%);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    mix-blend-mode: difference;
    transition: all 0.1s ease;
    transform: translate(-50%, -50%);
  }

  .enhanced-cursor.hover {
    transform: translate(-50%, -50%) scale(2);
    background: radial-gradient(circle, rgba(236, 72, 153, 0.8) 0%, rgba(79, 70, 229, 0.4) 100%);
  }

  /* Magnetic effect for interactive elements */
  .magnetic {
    transition: transform 0.3s ease;
  }

  .magnetic:hover {
    transform: scale(1.05);
  }
</style>

<!-- Loading Overlay -->
<div class="loading-overlay" id="loading-overlay">
  <div class="loading-content">
    <div class="loading-logo"></div>
    <div class="loading-text">EduNavigator</div>
    <div class="loading-bar">
      <div class="loading-progress" id="loading-progress"></div>
    </div>
  </div>
</div>

<!-- Enhanced Cursor -->
<div class="enhanced-cursor" id="enhanced-cursor"></div>

<!-- Loading and Enhanced Cursor JavaScript -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Loading animation
    const loadingOverlay = document.getElementById('loading-overlay');
    const loadingProgress = document.getElementById('loading-progress');
    
    let progress = 0;
    const loadingInterval = setInterval(() => {
      progress += Math.random() * 15;
      if (progress >= 100) {
        progress = 100;
        clearInterval(loadingInterval);
        
        setTimeout(() => {
          loadingOverlay.classList.add('hidden');
          document.body.classList.add('loaded');
          
          // Trigger page entrance animations
          const entranceElements = document.querySelectorAll('.hero-3d, .service-section-3d, .features-section, .how-it-works, .dream-career, .faq-section');
          entranceElements.forEach((el, index) => {
            setTimeout(() => {
              el.classList.add('page-entrance');
              setTimeout(() => el.classList.add('visible'), 100);
            }, index * 200);
          });
        }, 500);
      }
      loadingProgress.style.width = progress + '%';
    }, 100);

    // Enhanced cursor
    const enhancedCursor = document.getElementById('enhanced-cursor');
    
    document.addEventListener('mousemove', (e) => {
      enhancedCursor.style.left = e.clientX + 'px';
      enhancedCursor.style.top = e.clientY + 'px';
    });

    // Add magnetic effect to interactive elements
    const magneticElements = document.querySelectorAll('a, button, .service-card-3d, .fab-main, .cta-btn-3d');
    magneticElements.forEach(el => {
      el.classList.add('magnetic');
      
      el.addEventListener('mouseenter', () => {
        enhancedCursor.classList.add('hover');
      });
      
      el.addEventListener('mouseleave', () => {
        enhancedCursor.classList.remove('hover');
      });
    });

    // Add ripple effect to buttons
    function createRipple(event) {
      const button = event.currentTarget;
      const ripple = document.createElement('span');
      const rect = button.getBoundingClientRect();
      const size = Math.max(rect.width, rect.height);
      const x = event.clientX - rect.left - size / 2;
      const y = event.clientY - rect.top - size / 2;
      
      ripple.style.cssText = `
        position: absolute;
        width: ${size}px;
        height: ${size}px;
        left: ${x}px;
        top: ${y}px;
        background: rgba(255, 255, 255, 0.3);
        border-radius: 50%;
        transform: scale(0);
        animation: ripple 0.6s ease-out;
        pointer-events: none;
      `;
      
      button.appendChild(ripple);
      
      setTimeout(() => {
        ripple.remove();
      }, 600);
    }

    // Add ripple effect CSS
    const rippleStyle = document.createElement('style');
    rippleStyle.textContent = `
      @keyframes ripple {
        to {
          transform: scale(4);
          opacity: 0;
        }
      }
    `;
    document.head.appendChild(rippleStyle);

    // Apply ripple effect to buttons
    const buttons = document.querySelectorAll('.cta-btn-3d, .fab-main, .scroll-top');
    buttons.forEach(button => {
      button.addEventListener('click', createRipple);
    });

    // Add scroll-triggered animations
    const scrollAnimations = () => {
      const scrolled = window.pageYOffset;
      const parallaxElements = document.querySelectorAll('.floating-3d, .service-particles, .bg-element');
      
      parallaxElements.forEach((el, index) => {
        const speed = (index + 1) * 0.3;
        const yPos = -(scrolled * speed);
        el.style.transform = `translateY(${yPos}px)`;
      });
    };

    window.addEventListener('scroll', scrollAnimations);
  });
</script>

<!-- Enhanced 3D Interactive Hero Section with Video Background -->
<style>
  /* 3D Interactive Hero Styles */
  .hero-3d {
        min-height: 100vh;
        background: linear-gradient(135deg, #0f172a 0%, #1e1b4b 100%);
        display: flex;
        align-items: center;
        position: relative;
        overflow: hidden;
        padding: 4rem 2rem;
    perspective: 1000px;
    }

  /* 3D Background Elements */
  .hero-3d-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(15, 23, 42, 0.8) 0%, rgba(30, 27, 75, 0.9) 100%);
    z-index: 1;
  }

  /* 3D Floating Elements */
  .floating-3d-elements {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    transform-style: preserve-3d;
    z-index: 2;
  }

  /* Particle System */
  .particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 2;
  }

  .particle {
    position: absolute;
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border-radius: 50%;
    opacity: 0.6;
    animation: float-particle 8s ease-in-out infinite;
  }

  .particle:nth-child(1) { width: 8px; height: 8px; top: 20%; left: 10%; animation-delay: 0s; }
  .particle:nth-child(2) { width: 12px; height: 12px; top: 60%; left: 80%; animation-delay: 1s; }
  .particle:nth-child(3) { width: 6px; height: 6px; top: 80%; left: 20%; animation-delay: 2s; }
  .particle:nth-child(4) { width: 10px; height: 10px; top: 30%; left: 70%; animation-delay: 3s; }
  .particle:nth-child(5) { width: 14px; height: 14px; top: 70%; left: 30%; animation-delay: 4s; }

  @keyframes float-particle {
    0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.6; }
    50% { transform: translateY(-20px) rotate(180deg); opacity: 1; }
  }

  /* 3D Grid Background */
  .grid-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(rgba(79, 70, 229, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(79, 70, 229, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: grid-move 20s linear infinite;
    transform: perspective(1000px) rotateX(60deg);
    z-index: 1;
  }

  @keyframes grid-move {
    0% { transform: perspective(1000px) rotateX(60deg) translateY(0); }
    100% { transform: perspective(1000px) rotateX(60deg) translateY(50px); }
  }

  .hero-content-3d {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
        position: relative;
    z-index: 3;
    }

  .hero-text-3d {
        color: #fff;
    transform-style: preserve-3d;
    transition: transform 0.3s ease;
  }

  .hero-text-3d:hover {
    transform: translateZ(20px);
  }

  .hero-subtitle-3d {
    font-size: 1.2rem;
    color: #ec4899;
    font-weight: 600;
    margin-bottom: 1rem;
    opacity: 0;
    animation: slideInLeft 0.8s ease forwards;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .hero-title-3d {
        font-size: 4rem;
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    opacity: 0;
    animation: slideInLeft 0.8s ease 0.2s forwards;
    transform: translateZ(30px);
    }

  .hero-title-3d span {
        display: block;
        color: #fff;
        -webkit-text-fill-color: #fff;
    animation: glow 2s ease-in-out infinite alternate;
  }

  @keyframes glow {
    from { text-shadow: 0 0 20px rgba(236, 72, 153, 0.5); }
    to { text-shadow: 0 0 30px rgba(236, 72, 153, 0.8), 0 0 40px rgba(236, 72, 153, 0.6); }
  }

  .hero-description-3d {
        font-size: 1.25rem;
        line-height: 1.6;
        color: #94a3b8;
        margin-bottom: 2rem;
        opacity: 0;
    animation: slideInLeft 0.8s ease 0.4s forwards;
    }

  .cta-container-3d {
        display: flex;
        gap: 1rem;
        opacity: 0;
    animation: slideInLeft 0.8s ease 0.6s forwards;
    }

  .cta-btn-3d {
        display: inline-flex;
        align-items: center;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
        color: #fff;
        text-decoration: none;
        border-radius: 50px;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
    position: relative;
    overflow: hidden;
    transform-style: preserve-3d;
  }

  .cta-btn-3d::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .cta-btn-3d:hover::before {
    left: 100%;
  }

  .cta-btn-3d:hover {
    transform: translateY(-3px) translateZ(10px);
    box-shadow: 0 20px 40px rgba(79, 70, 229, 0.4);
  }

  .cta-btn-secondary-3d {
        background: transparent;
        border: 2px solid #4f46e5;
    backdrop-filter: blur(10px);
  }

  .cta-btn-secondary-3d:hover {
    background: rgba(79, 70, 229, 0.1);
    border-color: #ec4899;
  }

  .hero-image-3d {
    position: relative;
    transform-style: preserve-3d;
    animation: float3D 4s ease-in-out infinite;
  }

  .hero-image-3d:hover {
    animation-play-state: paused;
  }

  @keyframes float3D {
    0%, 100% { transform: translateZ(0) rotateY(0deg); }
    25% { transform: translateZ(20px) rotateY(5deg); }
    50% { transform: translateZ(40px) rotateY(0deg); }
    75% { transform: translateZ(20px) rotateY(-5deg); }
  }

  .hero-image-3d img {
        width: 100%;
        height: auto;
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    transition: all 0.3s ease;
  }

  .hero-image-3d:hover img {
    transform: scale(1.05);
    box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4);
  }

  /* 3D Floating Elements */
  .floating-3d {
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
    transform-style: preserve-3d;
    z-index: 2;
    }

  .floating-element-3d {
        position: absolute;
        background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
        border-radius: 50%;
        opacity: 0.1;
    animation: float3DElement 8s ease-in-out infinite;
    transform-style: preserve-3d;
  }

  .element-3d-1 { 
    width: 120px; height: 120px; top: 15%; left: 15%; 
    animation-delay: 0s; transform: translateZ(50px); 
  }
  .element-3d-2 { 
    width: 80px; height: 80px; top: 65%; right: 15%; 
    animation-delay: 1s; transform: translateZ(30px); 
  }
  .element-3d-3 { 
    width: 100px; height: 100px; bottom: 25%; left: 25%; 
    animation-delay: 2s; transform: translateZ(40px); 
  }

  @keyframes float3DElement {
    0%, 100% { transform: translateZ(50px) rotateY(0deg) rotateX(0deg); }
    33% { transform: translateZ(70px) rotateY(120deg) rotateX(20deg); }
    66% { transform: translateZ(60px) rotateY(240deg) rotateX(-20deg); }
  }

  /* 3D Animations */
  @keyframes slideInLeft {
        from {
            opacity: 0;
      transform: translateX(-50px) translateZ(0);
        }
        to {
            opacity: 1;
      transform: translateX(0) translateZ(0);
    }
  }

  /* Interactive Mouse Movement */
  .hero-3d {
    cursor: none;
  }

  .custom-cursor {
    position: fixed;
    width: 20px;
    height: 20px;
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border-radius: 50%;
    pointer-events: none;
    z-index: 9999;
    transition: transform 0.1s ease;
    mix-blend-mode: difference;
  }

  .custom-cursor.hover {
    transform: scale(2);
  }

  /* Responsive Design */
    @media (max-width: 992px) {
    .hero-content-3d {
            grid-template-columns: 1fr;
            text-align: center;
            gap: 2rem;
        }

    .hero-title-3d {
            font-size: 3rem;
        }

    .cta-container-3d {
            justify-content: center;
        }

    .hero-image-3d {
            max-width: 80%;
            margin: 0 auto;
        }

    .floating-video {
      display: none;
    }
    }

    @media (max-width: 768px) {
    .hero-3d {
            padding: 2rem 1rem;
        }

    .hero-title-3d {
            font-size: 2.5rem;
        }

    .hero-description-3d {
            font-size: 1.1rem;
        }

    .custom-cursor {
      display: none;
    }
    }
</style>

<section class="hero-3d" id="hero-section">
  <!-- 3D Background Elements -->
  <div class="hero-3d-bg"></div>
  
  <!-- 3D Floating Elements -->
  <div class="floating-3d-elements">
    <div class="floating-element-3d element-3d-1"></div>
    <div class="floating-element-3d element-3d-2"></div>
    <div class="floating-element-3d element-3d-3"></div>
  </div>

  <!-- Particle System -->
  <div class="particles">
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
            </div>

  <!-- 3D Grid Background -->
  <div class="grid-bg"></div>

  <!-- 3D Floating Elements -->
  <div class="floating-3d">
    <div class="floating-element-3d element-3d-1"></div>
    <div class="floating-element-3d element-3d-2"></div>
    <div class="floating-element-3d element-3d-3"></div>
  </div>

  <div class="hero-content-3d">
    <div class="hero-text-3d">
      <h3 class="hero-subtitle-3d">Your Future Awaits</h3>
      <h1 class="hero-title-3d">Shape Your <span>Future Today</span></h1>
      <p class="hero-description-3d">Take the first step towards your ideal career path. Our AI-powered platform and expert mentors are here to guide you every step of the way.</p>
      <div class="cta-container-3d">
        <a href="#get-started" class="cta-btn-3d">Get Started Now <i class="fas fa-arrow-right ml-2"></i></a>
        <a href="#learn-more" class="cta-btn-3d cta-btn-secondary-3d">Learn More</a>
      </div>
    </div>
    <div class="hero-image-3d">
      <img src="/img/App_bg1.png" alt="Career Guidance" />
        </div>
    </div>
</section>

<!-- Custom Cursor -->
<div class="custom-cursor"></div>

<!-- Interactive JavaScript for 3D Effects -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Custom cursor effect
    const cursor = document.querySelector('.custom-cursor');
    const heroSection = document.querySelector('.hero-3d');
    
    document.addEventListener('mousemove', (e) => {
      cursor.style.left = e.clientX + 'px';
      cursor.style.top = e.clientY + 'px';
    });

    // Add hover effect to interactive elements
    const interactiveElements = document.querySelectorAll('.cta-btn-3d, .hero-image-3d');
    interactiveElements.forEach(el => {
      el.addEventListener('mouseenter', () => cursor.classList.add('hover'));
      el.addEventListener('mouseleave', () => cursor.classList.remove('hover'));
    });

    // 3D Parallax effect on mouse move
    heroSection.addEventListener('mousemove', (e) => {
      const { clientX, clientY } = e;
      const { innerWidth, innerHeight } = window;
      
      const xAxis = (clientX / innerWidth - 0.5) * 20;
      const yAxis = (clientY / innerHeight - 0.5) * 20;
      
      const floatingElements = document.querySelectorAll('.floating-element-3d');
      floatingElements.forEach((el, index) => {
        const speed = (index + 1) * 0.5;
        el.style.transform = `translateZ(${50 + index * 20}px) rotateY(${xAxis * speed}deg) rotateX(${yAxis * speed}deg)`;
      });
    });

    // Smooth scroll animation for CTA buttons
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Intersection Observer for animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.animationPlayState = 'running';
        }
      });
    }, observerOptions);

    // Observe all animated elements
    document.querySelectorAll('.particle, .floating-element-3d').forEach(el => {
      observer.observe(el);
    });
  });
</script>



<!-- Enhanced 3D Interactive Service Section -->
<style>
  .service-section-3d {
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    padding: 8rem 0;
    position: relative;
    overflow: hidden;
    perspective: 1000px;
  }

  .service-section-3d::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
      radial-gradient(circle at 20% 80%, rgba(79, 70, 229, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(236, 72, 153, 0.1) 0%, transparent 50%);
    animation: backgroundShift 15s ease-in-out infinite;
  }

  @keyframes backgroundShift {
    0%, 100% { transform: scale(1) rotate(0deg); }
    50% { transform: scale(1.1) rotate(5deg); }
  }

  .service-header-3d {
    text-align: center;
    margin-bottom: 5rem;
    position: relative;
    z-index: 2;
  }

  .service-subtitle-3d {
    color: #6366f1;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 1rem;
    opacity: 0;
    animation: slideInDown 0.8s ease forwards;
    position: relative;
  }

  .service-subtitle-3d::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    animation: expandLine 0.8s ease 0.5s forwards;
  }

  @keyframes expandLine {
    to { width: 100px; }
  }

  .service-title-3d {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 2rem;
    opacity: 0;
    animation: slideInDown 0.8s ease 0.2s forwards;
    transform: translateZ(20px);
  }

  .service-grid-3d {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 3rem;
    padding: 0 2rem;
    position: relative;
    z-index: 2;
  }

  .service-card-3d {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 25px;
    padding: 2.5rem;
    transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(99, 102, 241, 0.1);
    opacity: 0;
    animation: fadeInScale 0.8s ease forwards;
    transform-style: preserve-3d;
    backdrop-filter: blur(10px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    min-height: 400px;
  }



  .service-card-3d::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(79, 70, 229, 0.05) 0%, rgba(236, 72, 153, 0.05) 100%);
    opacity: 0;
    transition: all 0.5s ease;
    transform: scale(0.8);
    z-index: -1;
  }

  .service-card-3d:hover::before {
    opacity: 1;
    transform: scale(1);
  }

  .service-card-3d:hover {
    transform: translateY(-15px) translateZ(30px) rotateX(5deg);
    box-shadow: 0 30px 60px rgba(0, 0, 0, 0.15);
    border-color: rgba(99, 102, 241, 0.3);
  }

  .service-icon-3d {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 2rem;
    transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    position: relative;
    transform-style: preserve-3d;
    z-index: 1;
  }

  .service-card-3d:hover .service-icon-3d {
    transform: rotateY(15deg) rotateX(15deg) scale(1.2);
    box-shadow: 0 15px 35px rgba(79, 70, 229, 0.4);
  }

  .service-icon-3d::after {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(135deg, #4f46e5, #ec4899, #4f46e5);
    border-radius: 22px;
    z-index: -1;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .service-card-3d:hover .service-icon-3d::after {
    opacity: 1;
    animation: rotate 2s linear infinite;
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .service-icon-3d svg {
    width: 40px;
    height: 40px;
    color: white;
    transition: all 0.3s ease;
  }

  .service-card-3d:hover .service-icon-3d svg {
    transform: scale(1.1);
  }

  .service-name-3d {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: #1f2937;
    transition: all 0.3s ease;
    position: relative;
    z-index: 1;
  }

  .service-card-3d:hover .service-name-3d {
    color: #4f46e5;
    transform: translateX(10px);
  }

  .service-description-3d {
    color: #6b7280;
    line-height: 1.7;
    margin-bottom: 2rem;
    transition: all 0.3s ease;
    position: relative;
    z-index: 1;
  }

  .service-card-3d:hover .service-description-3d {
    color: #374151;
  }

  .service-link-3d {
    display: inline-flex;
    align-items: center;
    color: #4f46e5;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    z-index: 1;
  }

  .service-link-3d::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
    transition: width 0.3s ease;
  }

  .service-link-3d:hover::before {
    width: 100%;
  }

  .service-link-3d:hover {
    color: #ec4899;
    transform: translateX(10px);
  }

  .service-link-3d svg {
    margin-left: 0.5rem;
    transition: all 0.3s ease;
  }

  .service-link-3d:hover svg {
    transform: translateX(5px) scale(1.1);
  }

  /* Floating particles for service section */
  .service-particles {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    pointer-events: none;
  }

  .service-particle {
    position: absolute;
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border-radius: 50%;
    opacity: 0.3;
    animation: floatService 10s ease-in-out infinite;
  }

  .service-particle:nth-child(1) { width: 6px; height: 6px; top: 15%; left: 5%; animation-delay: 0s; }
  .service-particle:nth-child(2) { width: 8px; height: 8px; top: 75%; right: 10%; animation-delay: 2s; }
  .service-particle:nth-child(3) { width: 4px; height: 4px; bottom: 20%; left: 15%; animation-delay: 4s; }

  @keyframes floatService {
    0%, 100% { transform: translateY(0) rotate(0deg); opacity: 0.3; }
    50% { transform: translateY(-15px) rotate(180deg); opacity: 0.6; }
  }

  @keyframes slideInDown {
    from {
      opacity: 0;
      transform: translateY(-30px) translateZ(0);
    }
    to {
      opacity: 1;
      transform: translateY(0) translateZ(0);
    }
  }

  @keyframes fadeInScale {
    from {
      opacity: 0;
      transform: scale(0.8) translateY(20px);
    }
    to {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }

  /* Interactive hover effects */
  .service-card-3d {
    cursor: pointer;
  }

  .service-card-3d:active {
    transform: translateY(-5px) translateZ(15px) scale(0.98);
  }

  @media (max-width: 768px) {
    .service-section-3d {
      padding: 6rem 0;
    }

    .service-title-3d {
      font-size: 2.5rem;
    }

    .service-grid-3d {
      grid-template-columns: 1fr;
      gap: 2rem;
      padding: 0 1rem;
    }

    .service-card-3d {
      padding: 2rem;
    }
  }
</style>

<section class="service-section-3d" id="services">
  <!-- Floating particles -->
  <div class="service-particles">
    <div class="service-particle"></div>
    <div class="service-particle"></div>
    <div class="service-particle"></div>
  </div>

  <div class="container">
    <div class="service-header-3d">
      <h3 class="service-subtitle-3d">What We Do</h3>
      <h2 class="service-title-3d">Empowering Your Career Journey</h2>
    </div>

    <div class="service-grid-3d">
      <div class="service-card-3d" style="animation-delay: 0.3s;">
        <div class="service-icon-3d">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
          </svg>
        </div>
        <h3 class="service-name-3d">AI Career Counseling</h3>
        <p class="service-description-3d">Get personalized career guidance powered by advanced AI algorithms that analyze your skills, interests, and potential to suggest the best career paths.</p>
        <a href="#!" class="service-link-3d">
          Explore More
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
            <path d="M4 8a.5.5 0 0 1 .5-.5h5.793L8.146 5.354a.5.5 0 1 1 .708-.708l3 3a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708-.708L10.293 8.5H4.5A.5.5 0 0 1 4 8z"/>
          </svg>
        </a>
      </div>

      <div class="service-card-3d" style="animation-delay: 0.4s;">
        <div class="service-icon-3d">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
          </svg>
        </div>
        <h3 class="service-name-3d">College Recommendations</h3>
        <p class="service-description-3d">Discover the perfect educational institutions that align with your academic performance, career goals, and personal preferences.</p>
        <a href="#!" class="service-link-3d">
          Learn More
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
            <path d="M4 8a.5.5 0 0 1 .5-.5h5.793L8.146 5.354a.5.5 0 1 1 .708-.708l3 3a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708-.708L10.293 8.5H4.5A.5.5 0 0 1 4 8z"/>
          </svg>
        </a>
      </div>

      <div class="service-card-3d" style="animation-delay: 0.5s;">
        <div class="service-icon-3d">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
          </svg>
        </div>
        <h3 class="service-name-3d">Aptitude Assessment</h3>
        <p class="service-description-3d">Take our comprehensive AI-driven assessment to evaluate your skills, interests, and career preferences.</p>
        <a href="#!" class="service-link-3d">
          Start Now
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
            <path d="M4 8a.5.5 0 0 1 .5-.5h5.793L8.146 5.354a.5.5 0 1 1 .708-.708l3 3a.5.5 0 0 1 0 .708l-3 3a.5.5 0 0 1-.708-.708L10.293 8.5H4.5A.5.5 0 0 1 4 8z"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<!-- Interactive JavaScript for Service Cards -->
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // 3D tilt effect for service cards
    const serviceCards = document.querySelectorAll('.service-card-3d');
    
    serviceCards.forEach(card => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        
        const centerX = rect.width / 2;
        const centerY = rect.height / 2;
        
        const rotateX = (y - centerY) / 10;
        const rotateY = (centerX - x) / 10;
        
        card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateZ(20px)`;
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'perspective(1000px) rotateX(0deg) rotateY(0deg) translateZ(0px)';
      });
    });

    // Intersection Observer for animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.animationPlayState = 'running';
        }
      });
    }, observerOptions);

    // Observe service cards
    serviceCards.forEach(card => {
      observer.observe(card);
    });
  });
</script>


  <!-- Features Section - Modern Design -->
  <style>
    .features-section {
        background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
        padding: 8rem 0;
        position: relative;
        overflow: hidden;
        color: white;
    }

    .features-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: url('/img/grid-pattern.png') repeat;
        opacity: 0.05;
        animation: backgroundMove 20s linear infinite;
    }

    .features-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        position: relative;
        z-index: 1;
    }

    .features-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .features-subtitle {
        color: #ec4899;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 1rem;
        opacity: 0;
        animation: fadeInUp 0.6s ease forwards;
    }

    .features-title {
        font-size: 2.5rem;
        font-weight: 800;
        background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1.5rem;
        opacity: 0;
        animation: fadeInUp 0.6s ease 0.2s forwards;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 20px;
        padding: 2.5rem;
        text-align: center;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
        opacity: 0;
        animation: fadeInUp 0.6s ease forwards;
        backdrop-filter: blur(10px);
        position: relative;
        overflow: hidden;
        min-height: 350px;
    }



    .feature-card:hover {
        transform: translateY(-10px);
        background: rgba(255, 255, 255, 0.1);
        border-color: #ec4899;
    }

    .feature-icon {
        width: 70px;
        height: 70px;
        margin: 0 auto 1.5rem;
        background: linear-gradient(135deg, #4f46e5, #ec4899);
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        color: white;
        transition: all 0.3s ease;
        position: relative;
        z-index: 1;
    }

    .feature-card:hover .feature-icon {
        transform: rotate(10deg) scale(1.1);
    }

    .feature-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: white;
        margin-bottom: 1rem;
        position: relative;
        z-index: 1;
    }

    .feature-description {
        color: #94a3b8;
        line-height: 1.6;
        position: relative;
        z-index: 1;
    }

    @media (max-width: 768px) {
        .features-section {
            padding: 4rem 0;
        }

        .features-title {
            font-size: 2rem;
        }

        .feature-card {
            padding: 2rem;
        }
    }
  </style>

  <section class="features-section">
    <div class="features-container">
        <div class="features-header">
            <h3 class="features-subtitle">Why Choose Us</h3>
            <h2 class="features-title">Why Choose EduNavigator?</h2>
        </div>

        <div class="features-grid">
            <div class="feature-card" style="animation-delay: 0.3s;">
                <div class="feature-icon">
                    <i class="fas fa-user-graduate"></i>
                </div>
                <h3 class="feature-title">Personalized Guidance</h3>
                <p class="feature-description">Get expert advice tailored to your unique skills, interests, and career aspirations through our AI-powered platform.</p>
            </div>

            <div class="feature-card" style="animation-delay: 0.4s;">
                <div class="feature-icon">
                    <i class="fas fa-chart-line"></i>
                </div>
                <h3 class="feature-title">Career Assessments</h3>
                <p class="feature-description">Take comprehensive assessments to discover your strengths and find the perfect career path aligned with your potential.</p>
            </div>

            <div class="feature-card" style="animation-delay: 0.5s;">
                <div class="feature-icon">
                    <i class="fas fa-briefcase"></i>
                </div>
                <h3 class="feature-title">Expert Mentorship</h3>
                <p class="feature-description">Connect with industry professionals and gain valuable insights through one-on-one mentoring sessions.</p>
            </div>
        </div>
    </div>
  </section>

  <!-- How It Works - Modern Design -->
  <style>
    .how-it-works {
      background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
      padding: 8rem 0;
      position: relative;
      overflow: hidden;
      color: white;
    }

    .how-it-works::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('/img/grid-pattern.png') repeat;
      opacity: 0.05;
      animation: backgroundMove 20s linear infinite;
    }

    .how-it-works-header {
      text-align: center;
      margin-bottom: 5rem;
      position: relative;
    }

    .how-it-works-subtitle {
      color: #ec4899;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 1rem;
      opacity: 0;
      animation: fadeInUp 0.6s ease forwards;
    }

    .how-it-works-title {
      font-size: 2.5rem;
      font-weight: 800;
      background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 1.5rem;
      opacity: 0;
      animation: fadeInUp 0.6s ease 0.2s forwards;
    }

    .how-it-works-description {
      color: #94a3b8;
      font-size: 1.1rem;
      max-width: 600px;
      margin: 0 auto;
      opacity: 0;
      animation: fadeInUp 0.6s ease 0.3s forwards;
    }

    .timeline-container {
      max-width: 1000px;
      margin: 0 auto;
      position: relative;
      padding: 0 2rem;
    }

    .timeline-line {
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      width: 4px;
      height: 100%;
      background: linear-gradient(to bottom, #4f46e5 0%, #ec4899 100%);
      opacity: 0.3;
    }

    .timeline-progress {
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      width: 4px;
      height: 0%;
      background: linear-gradient(to bottom, #4f46e5 0%, #ec4899 100%);
      transition: height 1s ease;
    }

    .timeline-steps {
      position: relative;
      z-index: 1;
    }

    .timeline-step {
      display: flex;
      align-items: center;
      margin-bottom: 4rem;
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 0.6s ease forwards;
    }

    .timeline-step:nth-child(odd) {
      flex-direction: row;
    }

    .timeline-step:nth-child(even) {
      flex-direction: row-reverse;
    }

    .timeline-content {
      width: calc(50% - 2rem);
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      padding: 2rem;
      position: relative;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
    }

    .timeline-content:hover {
      transform: translateY(-5px);
      background: rgba(255, 255, 255, 0.1);
      border-color: rgba(255, 255, 255, 0.2);
    }

    .timeline-number {
      width: 4rem;
      height: 4rem;
      background: linear-gradient(135deg, #4f46e5, #ec4899);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 1.5rem;
      margin: 0 2rem;
      position: relative;
      z-index: 2;
      box-shadow: 0 0 20px rgba(236, 72, 153, 0.3);
    }

    .timeline-step-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      background: linear-gradient(135deg, #fff 0%, #94a3b8 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .timeline-step-description {
      color: #94a3b8;
      line-height: 1.6;
    }

    .timeline-icon {
      width: 3rem;
      height: 3rem;
      margin-bottom: 1rem;
      color: #ec4899;
    }

    @media (max-width: 768px) {
      .timeline-line,
      .timeline-progress {
        left: 2rem;
      }

      .timeline-step {
        flex-direction: row !important;
        margin-left: 4rem;
      }

      .timeline-content {
        width: calc(100% - 6rem);
      }

      .timeline-number {
        margin: 0 1rem;
      }
    }

    @keyframes progressLine {
      to {
        height: 100%;
      }
    }
  </style>

  <section class="how-it-works">
    <div class="container">
      <div class="how-it-works-header">
        <h3 class="how-it-works-subtitle">How It Works</h3>
        <h2 class="how-it-works-title">Your Journey to Success</h2>
        <p class="how-it-works-description">Follow these simple steps to unlock your potential and discover your ideal career path with EduNavigator.</p>
      </div>

      <div class="timeline-container">
        <div class="timeline-line"></div>
        <div class="timeline-progress"></div>

        <div class="timeline-steps">
          <div class="timeline-step" style="animation-delay: 0.4s;">
            <div class="timeline-content">
              <svg class="timeline-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
              <h3 class="timeline-step-title">Sign Up</h3>
              <p class="timeline-step-description">Create your account and complete your profile with your educational background and career interests.</p>
            </div>
            <div class="timeline-number">1</div>
          </div>

          <div class="timeline-step" style="animation-delay: 0.6s;">
            <div class="timeline-content">
              <svg class="timeline-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              <h3 class="timeline-step-title">Take Assessment</h3>
              <p class="timeline-step-description">Complete our comprehensive AI-driven assessment to evaluate your skills, interests, and career preferences.</p>
            </div>
            <div class="timeline-number">2</div>
          </div>

          <div class="timeline-step" style="animation-delay: 0.8s;">
            <div class="timeline-content">
              <svg class="timeline-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
              </svg>
              <h3 class="timeline-step-title">Get Recommendations</h3>
              <p class="timeline-step-description">Receive personalized career recommendations and insights based on your assessment results.</p>
            </div>
            <div class="timeline-number">3</div>
          </div>

          <div class="timeline-step" style="animation-delay: 1s;">
            <div class="timeline-content">
              <svg class="timeline-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
              <h3 class="timeline-step-title">Connect with Mentors</h3>
              <p class="timeline-step-description">Book sessions with experienced mentors and industry professionals for guidance and insights.</p>
            </div>
            <div class="timeline-number">4</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.querySelector('.timeline-progress').style.height = '100%';
          }
        });
      }, { threshold: 0.5 });

      observer.observe(document.querySelector('.timeline-container'));
    });
  </script>


  <!-- Dream Career Banner - Modern Design -->
  <style>
    .dream-career {
      background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
      padding: 8rem 0;
      position: relative;
      overflow: hidden;
      color: white;
    }

    .dream-career::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('/img/grid-pattern.png') repeat;
      opacity: 0.05;
      animation: backgroundMove 20s linear infinite;
    }

    .dream-career-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
      position: relative;
      z-index: 1;
    }

    .dream-career-content {
      position: relative;
      text-align: center;
    }

    .dream-career-subtitle {
      font-size: 1.5rem;
      font-weight: 600;
      color: #ec4899;
      margin-bottom: 1.5rem;
      opacity: 0;
      animation: fadeInUp 0.6s ease forwards;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .dream-career-title {
      font-size: 4rem;
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: 2rem;
      background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      opacity: 0;
      animation: fadeInUp 0.6s ease 0.2s forwards;
    }

    .dream-career-description {
      font-size: 1.25rem;
      color: #94a3b8;
      max-width: 600px;
      margin: 0 auto 3rem;
      line-height: 1.8;
      opacity: 0;
      animation: fadeInUp 0.6s ease 0.4s forwards;
    }

    .dream-career-cta {
      display: flex;
      gap: 1.5rem;
      justify-content: center;
      opacity: 0;
      animation: fadeInUp 0.6s ease 0.6s forwards;
    }

    .dream-career-btn {
      display: inline-flex;
      align-items: center;
      padding: 1rem 2.5rem;
      font-size: 1.1rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-radius: 50px;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .dream-career-btn-primary {
      background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
      color: white;
      border: none;
      box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
    }

    .dream-career-btn-secondary {
      background: transparent;
      color: white;
      border: 2px solid #4f46e5;
    }

    .dream-career-btn:hover {
      transform: translateY(-2px);
    }

    .dream-career-btn-primary:hover {
      box-shadow: 0 15px 30px rgba(79, 70, 229, 0.4);
    }

    .dream-career-btn-secondary:hover {
      background: rgba(79, 70, 229, 0.1);
      border-color: #ec4899;
    }

    .dream-career-shapes {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      pointer-events: none;
    }

    .dream-career-shape {
      position: absolute;
      background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
      border-radius: 50%;
      opacity: 0.1;
      animation: float 6s ease-in-out infinite;
    }

    .shape-1 {
      width: 150px;
      height: 150px;
      top: -75px;
      left: 10%;
      animation-delay: 0s;
    }

    .shape-2 {
      width: 100px;
      height: 100px;
      bottom: -50px;
      right: 15%;
      animation-delay: 1s;
    }

    .shape-3 {
      width: 75px;
      height: 75px;
      top: 50%;
      left: 5%;
      animation-delay: 2s;
    }

    @media (max-width: 768px) {
      .dream-career {
        padding: 6rem 0;
      }

      .dream-career-title {
        font-size: 2.5rem;
      }

      .dream-career-description {
        font-size: 1.1rem;
      }

      .dream-career-cta {
        flex-direction: column;
        align-items: center;
      }

      .dream-career-btn {
        width: 100%;
        max-width: 300px;
        justify-content: center;
      }
    }
  </style>

  <section class="dream-career">
    <div class="dream-career-shapes">
      <div class="dream-career-shape shape-1"></div>
      <div class="dream-career-shape shape-2"></div>
      <div class="dream-career-shape shape-3"></div>
    </div>
    
    <div class="dream-career-container">
      <div class="dream-career-content">
        <h3 class="dream-career-subtitle">Your Future Awaits</h3>
        <h2 class="dream-career-title">Explore Your Dream Career</h2>
        <p class="dream-career-description">
          Take the first step towards your ideal career path. Our AI-powered platform and expert mentors are here to guide you every step of the way.
        </p>
        <div class="dream-career-cta">
          <a href="#get-started" class="dream-career-btn dream-career-btn-primary">
            Get Started Now
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16" style="margin-left: 0.5rem;">
              <path d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8z"/>
            </svg>
          </a>
          <a href="#learn-more" class="dream-career-btn dream-career-btn-secondary">
            Learn More
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16" style="margin-left: 0.5rem;">
              <path d="M8 0a8 8 0 1 1 0 16A8 8 0 0 1 8 0zM4.5 7.5a.5.5 0 0 0 0 1h5.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5H4.5A.5.5 0 0 1 4 8z"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

   
   <!-- FAQ Section - Modern Design -->
   <style>
     .faq-section {
       background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
       padding: 8rem 0;
       position: relative;
       overflow: hidden;
       color: white;
     }

     .faq-section::before {
       content: '';
       position: absolute;
       top: 0;
       left: 0;
       width: 100%;
       height: 100%;
       background: url('/img/grid-pattern.png') repeat;
       opacity: 0.05;
       animation: backgroundMove 20s linear infinite;
     }

     .faq-container {
       max-width: 800px;
       margin: 0 auto;
       padding: 0 2rem;
       position: relative;
       z-index: 1;
     }

    

     .faq-header {
       text-align: center;
       margin-bottom: 4rem;
     }

     .faq-subtitle {
       color: #ec4899;
       font-weight: 700;
       text-transform: uppercase;
       letter-spacing: 2px;
       margin-bottom: 1rem;
       opacity: 0;
       animation: fadeInUp 0.6s ease forwards;
     }

     .faq-title {
       font-size: 2.5rem;
       font-weight: 800;
       background: linear-gradient(135deg, #4f46e5 0%, #ec4899 100%);
       -webkit-background-clip: text;
       -webkit-text-fill-color: transparent;
       margin-bottom: 1.5rem;
       opacity: 0;
       animation: fadeInUp 0.6s ease 0.2s forwards;
     }

     .faq-description {
       color: #94a3b8;
       font-size: 1.1rem;
       max-width: 600px;
       margin: 0 auto;
       line-height: 1.8;
       opacity: 0;
       animation: fadeInUp 0.6s ease 0.3s forwards;
     }

     .faq-list {
       display: flex;
       flex-direction: column;
       gap: 1rem;
     }

     .faq-item {
       background: rgba(255, 255, 255, 0.05);
       border-radius: 16px;
       overflow: hidden;
       box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
       opacity: 0;
       animation: fadeInUp 0.6s ease forwards;
       border: 1px solid rgba(255, 255, 255, 0.1);
       transition: all 0.3s ease;
       backdrop-filter: blur(10px);
     }

     .faq-item:hover {
       transform: translateY(-2px);
       box-shadow: 0 8px 12px rgba(0, 0, 0, 0.3);
       border-color: rgba(236, 72, 153, 0.3);
     }

     .faq-question {
       padding: 1.5rem;
       display: flex;
       align-items: center;
       justify-content: space-between;
       cursor: pointer;
       font-weight: 600;
       color: #fff;
       transition: all 0.3s ease;
     }

     .faq-question:hover {
       color: #ec4899;
     }

     .faq-icon {
       width: 24px;
       height: 24px;
       position: relative;
       transition: all 0.3s ease;
     }

     .faq-icon::before,
     .faq-icon::after {
       content: '';
       position: absolute;
       background: #ec4899;
       border-radius: 2px;
       transition: all 0.3s ease;
     }

     .faq-icon::before {
       width: 2px;
       height: 16px;
       top: 4px;
       left: 11px;
     }

     .faq-icon::after {
       width: 16px;
       height: 2px;
       top: 11px;
       left: 4px;
     }

     .faq-answer {
       padding: 0 1.5rem;
       height: 0;
       overflow: hidden;
       color: #94a3b8;
       line-height: 1.6;
       transition: all 0.3s ease;
     }

     details[open] .faq-icon::before {
       transform: rotate(90deg);
       opacity: 0;
     }

     details[open] .faq-answer {
       padding: 0 1.5rem 1.5rem;
       height: auto;
     }

     details[open] .faq-question {
       color: #ec4899;
     }

     .faq-answer ul {
       margin-top: 1rem;
       padding-left: 1.5rem;
     }

     .faq-answer li {
       margin-bottom: 0.5rem;
       position: relative;
     }

     .faq-answer li::before {
       content: '•';
       color: #ec4899;
       position: absolute;
       left: -1rem;
     }

     @media (max-width: 768px) {
       .faq-section {
         padding: 4rem 0;
       }

       .faq-title {
         font-size: 2rem;
       }

       .faq-description {
         font-size: 1rem;
       }
     }
   </style>

   
   <!-- Quick Help Button -->
<div class="quick-help-button" id="quick-help-button" onclick="toggleChatbot()">
  <i class="fas fa-question-circle"></i>
</div>

<!-- Chatbot UI -->
<div class="chatbot-container hidden" id="chatbot-container">
  <div class="chatbot-header">
    EduNavigator Chatbot
    <button class="close-chatbot" onclick="toggleChatbot()">×</button>
  </div>
  <div class="chatbot-messages" id="chatbot-messages">
    <p class="bot-message">Hi! How can I assist you today?</p>
  </div>
  <div class="chatbot-predefined">
    <p>Quick Help:</p>
    <button onclick="sendPredefinedMessage('I want to give an interview')">I want to give an interview</button>
    <button onclick="sendPredefinedMessage('How can I prepare for an interview')">How can I prepare for an interview</button>
    <button onclick="sendPredefinedMessage('Where can I find career resources')">Where can I find career resources</button>
    <button onclick="sendPredefinedMessage('How can I access webinars')">How can I access webinars</button>
    <button onclick="sendPredefinedMessage('Where can I find mentors')">Where can I find mentors</button>
    <button onclick="sendPredefinedMessage('How can I access aptitude tests')">How can I access aptitude tests</button>
    <button onclick="sendPredefinedMessage('Where can I find job opportunities')">Where can I find job opportunities</button>
    <button onclick="sendPredefinedMessage('How can I access career trends')">How can I access career trends</button>
    <button onclick="sendPredefinedMessage('Where can I find scholarships')">Where can I find scholarships</button>
    <button onclick="sendPredefinedMessage('How can I contact support')">How can I contact support</button>
  </div>
  <div class="chatbot-input">
    <input type="text" id="chatbot-input" placeholder="Type your message..." />
    <button onclick="sendMessage()">Send</button>
  </div>
</div>

<!-- Styles for Quick Help Button and Chatbot -->
<style>
  .quick-help-button {
    position: fixed;
    bottom: 20px;
    right: 20px;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    cursor: pointer;
    z-index: 1000;
  }

  .quick-help-button:hover {
    background: linear-gradient(135deg, #ec4899, #4f46e5);
  }

  .chatbot-container {
    position: fixed;
    bottom: 90px;
    right: 20px;
    width: 400px; /* Increased width for better visibility */
    max-height: 600px; /* Increased height */
    background: linear-gradient(135deg, #1e1b4b, #0f172a);
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    z-index: 1000;
    transition: transform 0.3s ease, opacity 0.3s ease;
  }

  .chatbot-header {
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    color: white;
    padding: 1.5rem; /* Increased padding */
    font-size: 1.5rem; /* Increased font size */
    font-weight: bold;
    text-align: center;
    position: relative;
  }

  .chatbot-messages {
    flex: 1;
    padding: 1.5rem; /* Increased padding */
    overflow-y: auto;
    background: #0f172a;
    color: white;
    font-size: 1.3rem; /* Increased font size */
    line-height: 2; /* Adjusted line height for better readability */
  }

  .chatbot-messages p {
    margin: 1rem 0; /* Increased margin for spacing */
    padding: 1rem; /* Increased padding for better spacing */
    border-radius: 10px;
    max-width: 90%; /* Increased width */
  }

  .chatbot-messages .user-message {
    background: #4f46e5;
    color: white;
    align-self: flex-end;
    text-align: right;
    margin-left: auto;
  }

  .chatbot-messages .bot-message {
    background: #1e1b4b;
    color: #ec4899;
    align-self: flex-start;
    text-align: left;
    margin-right: auto;
  }

  .chatbot-predefined {
    padding: 1.5rem; /* Increased padding */
    background: #1e1b4b;
    color: white;
    text-align: center;
  }

  .chatbot-predefined p {
    margin-bottom: 1rem; /* Increased margin */
    font-size: 1.2rem; /* Increased font size */
    font-weight: bold;
  }

  .chatbot-predefined button {
    margin: 0.5rem; /* Increased margin */
    padding: 0.8rem 1.5rem; /* Increased padding */
    font-size: 1.2rem; /* Increased font size */
    background: linear-gradient(135deg, #4f46e5, #ec4899);
    border: none;
    border-radius: 10px;
    color: white;
    cursor: pointer;
  }

  .chatbot-predefined button:hover {
    background: linear-gradient(135deg, #ec4899, #4f46e5);
  }
</style>

<!-- JavaScript for Quick Help Button -->
<script>
  function toggleChatbot() {
    const chatbot = document.getElementById('chatbot-container');
    chatbot.classList.toggle('hidden');
  }

  function sendPredefinedMessage(message) {
    const input = document.getElementById('chatbot-input');
    input.value = message;
    sendMessage();
  }

  async function sendMessage() {
    const input = document.getElementById('chatbot-input');
    const messages = document.getElementById('chatbot-messages');
    const userMessage = input.value.trim();

    if (!userMessage) return;

    // Display user message
    const userMessageElement = document.createElement('p');
    userMessageElement.className = 'user-message';
    userMessageElement.textContent = userMessage;
    messages.appendChild(userMessageElement);

    // Clear input
    input.value = '';

    // Send message to backend
    try {
      const response = await fetch('/chat', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ message: userMessage }),
      });

      const data = await response.json();

      // Display bot response
      const botMessageElement = document.createElement('p');
      botMessageElement.className = 'bot-message';
      botMessageElement.textContent = data.reply;
      messages.appendChild(botMessageElement);

      // Scroll to the bottom
      messages.scrollTop = messages.scrollHeight;
    } catch (error) {
      console.error('Error:', error);
    }
  }
</script>
</body>
</html>